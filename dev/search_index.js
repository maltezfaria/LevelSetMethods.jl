var documenterSearchIndex = {"docs":
[{"category":"section","location":"narrow-band/#narrow-band","page":"Narrow-band fields","text":"The interface tracked by a level-set method is a lower-dimensional object — a curve in 2D, a surface in 3D — yet a dense MeshField stores and updates phi at every node of the grid. Away from the interface those values are irrelevant to the interface motion. A NarrowBandMeshField exploits this by keeping values only on a thin band of nodes around the interface, so the cost of a simulation scales with the size of the interface rather than the size of the grid. The saving grows with resolution and with dimension; it is most dramatic in 3D, where the grid has O(N^3) nodes but the band only O(N^2).\n\nA narrow-band field implements the same interface as a dense one (see Grids and mesh fields), so it is a drop-in replacement: equations, terms, boundary conditions, integrators, and geometric queries all work unchanged.","title":"Narrow-band fields"},{"category":"section","location":"narrow-band/#Constructing-a-band","page":"Narrow-band fields","text":"The usual way to build a band is to restrict an existing MeshField to a tube around its zero contour. The nlayers keyword sets the halo depth — how many node layers are kept around the cells the interface cuts through:\n\nusing LevelSetMethods\ngrid = CartesianGrid((-1, -1), (1, 1), (100, 100))\nϕ = MeshField(x -> hypot(x...) - 0.5, grid)\nnb = NarrowBandMeshField(ϕ; nlayers = 3)\n\nThe display reports the number of active nodes — those actually stored — out of the whole grid. Only a small fraction lies in the band:\n\nlength(nodeindices(nb)), length(active_nodeindices(nb))\n\nBand membership is purely topological (it is built from the cells whose corner values straddle zero), so ϕ need not be a signed distance function. It helps if it is, though: a distorted ϕ with nablaphi gg 1 packs its level sets together and makes the band thinner in physical space than nlayers suggests. Keeping ϕ close to a signed distance function (see Reinitialization) keeps the band well-behaved.","title":"Constructing a band"},{"category":"section","location":"narrow-band/#Solving-on-a-band","page":"Narrow-band fields","text":"Because a NarrowBandMeshField shares the dense-field interface, evolving one is exactly the same code as before — just pass it as the initial condition. Here we rotate a notched disk, plotting a few snapshots; the recipe shades the active band cells, so you can watch the band travel with the interface:\n\nusing GLMakie\nLevelSetMethods.set_makie_theme!()\n\ndisk = MeshField(x -> hypot((x .- (0.0, 0.4))...) - 0.3, grid)\nslot = MeshField(x -> maximum(abs.(x .- (0.0, 0.4)) .- (0.1, 0.4) ./ 2), grid)\nϕ₀ = setdiff(disk, slot)                       # a notched disk; see the geometry page\nnb₀ = NarrowBandMeshField(ϕ₀; nlayers = 3)\n\neq = LevelSetEquation(; terms = AdvectionTerm((x, t) -> (-x[2], x[1])), ic = nb₀, bc = NeumannBC())\nfig = Figure(; size = (1000, 280))\nfor (n, t) in enumerate((0.0, π / 2, π, 3π / 2))\n    integrate!(eq, t)\n    ax = Axis(fig[1, n]; title = \"t = $(round(t; digits = 2))\")\n    plot!(ax, eq)\nend\nfig","title":"Solving on a band"},{"category":"section","location":"narrow-band/#Automatic-band-maintenance","page":"Narrow-band fields","text":"As the interface moves, the band must move with it: nodes the interface is about to reach must be activated, and nodes it has left behind can be dropped. integrate! does this automatically — after every accepted step it calls update_band! to rebuild the tube around the new interface position. You do not need to manage the band yourself.\n\nThe nlayers halo must be deep enough for the finite-difference and WENO stencils, which reach a few nodes beyond the interface. The default of 3 is a sound choice for the WENO5 scheme; when a stencil reaches just past the band edge, the missing value is supplied by extrapolation from the nearest band nodes, so accuracy degrades gracefully rather than failing. A wider band trades memory for a little more accuracy near its edge.","title":"Automatic band maintenance"},{"category":"section","location":"narrow-band/#Reinitialization-on-a-band","page":"Narrow-band fields","text":"Reinitialization works on a band just as on a dense field, acting only on the active nodes. Because the band is rebuilt from the interface each step, keeping phi close to a signed distance function keeps the band's physical width predictable; a posthook reinitializing every step (or every few steps) is the usual pattern:\n\nintegrate!(eq, 2π; posthook = e -> reinitialize!(current_state(e)))\nLevelSetMethods.volume(current_state(eq))   # geometric queries work on the band too","title":"Reinitialization on a band"},{"category":"section","location":"narrow-band/#When-it-pays-off","page":"Narrow-band fields","text":"The band stores roughly the nodes within nlayers of the interface, so the active fraction falls as the grid is refined — the band is a fixed number of layers around a lower-dimensional set. In 3D the effect is striking, since the grid grows cubically while the band grows quadratically:\n\nfor n in (32, 48)\n    g = CartesianGrid((-1, -1, -1), (1, 1, 1), (n, n, n))\n    sphere = NarrowBandMeshField(MeshField(x -> hypot(x...) - 0.5, g); nlayers = 3)\n    frac = length(active_nodeindices(sphere)) / length(nodeindices(sphere))\n    println(\"n = $n:  $(length(active_nodeindices(sphere))) / $(length(nodeindices(sphere))) active  ($(round(100frac; digits = 1))%)\")\nend","title":"When it pays off"},{"category":"section","location":"narrow-band/#Limitations","page":"Narrow-band fields","text":"A few features are not available on a narrow band:\n\nPeriodic boundary conditions are not supported — the band and its halo do not wrap at the grid edge. Use a MeshField for periodic problems.\nThe SemiImplicitI2OE integrator requires a full-grid MeshField; the explicit integrators (ForwardEuler, RK2, RK3) all work on a band.","title":"Limitations"},{"category":"section","location":"extension-makie/#extension-makie","page":"Makie extension","text":"Loading a Makie backend activates plotting recipes for level sets. We recommend GLMakie for 3D plots and animations. Once a backend is loaded you can call plot directly on a MeshField, a NarrowBandMeshField, or a LevelSetEquation.\n\nLevelSetMethods ships a Theme with sensible defaults for these plots; apply it once with LevelSetMethods.set_makie_theme! (or fetch it with LevelSetMethods.makie_theme to scope it to a with_theme block).","title":"Makie extension"},{"category":"section","location":"extension-makie/#Two-dimensions","page":"Makie extension","text":"By default plot draws the zero contour and shades the interior phi  0:\n\nusing LevelSetMethods, GLMakie\nLevelSetMethods.set_makie_theme!()\ngrid = CartesianGrid((-2, -2), (2, 2), (64, 64))\nϕ = MeshField(grid) do x   # a star; see the geometry page\n    r, θ = hypot(x...), atan(x[2], x[1])\n    return r - (1 + 0.25 * cos(5θ))\nend\nplot(ϕ)\n\nFor more control, call Makie's contour (or contourf) directly — for example to draw several level sets at once:\n\ncontour(ϕ; levels = [-0.5, 0, 0.5], labels = true)","title":"Two dimensions"},{"category":"section","location":"extension-makie/#Three-dimensions","page":"Makie extension","text":"In 3D, plot renders the zero level set as an isosurface:\n\nusing LevelSetMethods, GLMakie, LinearAlgebra\nGLMakie.activate!()   # only GLMakie can render the volume isosurface\nLevelSetMethods.set_makie_theme!()\ngrid = CartesianGrid((-1.5, -1.5, -1.5), (1.5, 1.5, 1.5), (32, 32, 32))\nP1, P2 = (-1, 0, 0), (1, 0, 0)\nϕ = MeshField(x -> norm(x .- P1) * norm(x .- P2) - 1.05^2, grid)   # a Cassini surface\nplot(ϕ)\n\nAs in 2D, dropping down to Makie's volume gives full control — here drawing a different isovalue:\n\nMakie.volume(ϕ; algorithm = :iso, isovalue = 0.5)\n\nusing CairoMakie          # hide\nCairoMakie.activate!()    # restore the headless default for later pages # hide\nnothing                   # hide","title":"Three dimensions"},{"category":"section","location":"extension-makie/#Equations-and-narrow-bands","page":"Makie extension","text":"Calling plot on a LevelSetEquation plots its current_state — equivalent to plot(current_state(eq)) — which makes it convenient to animate a simulation by replotting as it advances (see the examples).\n\nThe recipe is band-aware: plotting a NarrowBandMeshField (or an equation whose state is one) shades the active cells in 2D and scatters the active nodes in 3D, so you can see the band travel with the interface — the narrow-band page shows this in action.","title":"Equations and narrow bands"},{"category":"section","location":"time-integrators/#time-integrators","page":"Time integration","text":"A LevelSetEquation is advanced in time by a time integrator, chosen with the integrator keyword (see Level-set equation). The following options are available:\n\nusing LevelSetMethods\nusing InteractiveUtils # hide\nsubtypes(LevelSetMethods.TimeIntegrator)\n\nForwardEuler, RK2, and RK3 are explicit schemes, and therefore a sufficiently small time step — dependent on the LevelSetTerms being used — is required to ensure stability. We recommend the second-order RK2 scheme (the default) for most applications. The package also provides SemiImplicitI2OE, a semi-implicit scheme ([3]) for advection equations.","title":"Time integration"},{"category":"section","location":"time-integrators/#The-CFL-number","page":"Time integration","text":"To stay stable, an explicit scheme cannot take an arbitrarily large time step: the step is bounded by a CFL condition set by the active terms and the mesh spacing. integrate! enforces this for you, computing the largest stable Δt at each step. Each integrator scales that limit by a cfl safety factor, passed at construction; a smaller factor takes smaller, more accurate (and more expensive) steps. It defaults to 0.5 for the explicit schemes:\n\nusing LevelSetMethods\nRK2(; cfl = 0.25)   # half the default step size\n\nYou can also cap the step from above by passing a maximum time step as the Δt argument of integrate!; the step actually taken is then the smaller of that cap and the CFL limit.","title":"The CFL number"},{"category":"section","location":"time-integrators/#Comparing-the-integrators","page":"Time integration","text":"To compare the schemes quantitatively, we rotate a dumbbell — built with the CSG operators from the geometry page — through one full revolution. Rigid rotation is area-preserving, so the exact final area equals the initial one; the leftover change in enclosed area then measures each scheme's accumulated error. Running every integrator at the same (default) CFL:\n\nusing LevelSetMethods, Markdown, Printf\n\ngrid = CartesianGrid((-1, -1), (1, 1), (64, 64))\ndisk(c) = MeshField(x -> hypot((x .- c)...) - 0.25, grid)\nbar     = MeshField(x -> maximum(abs.(x) .- (1.0, 0.2) ./ 2), grid)\nϕ₀ = disk((-0.5, 0.0)) ∪ disk((0.5, 0.0)) ∪ bar   # a dumbbell\n𝐮  = (x, t) -> (-x[2], x[1])\nV₀ = LevelSetMethods.volume(ϕ₀)                   # exact final area (rotation preserves it)\n\nresults = map((\"ForwardEuler\" => ForwardEuler(), \"RK2\" => RK2(), \"RK3\" => RK3())) do (name, integrator)\n    eq = LevelSetEquation(; terms = AdvectionTerm(𝐮), ic = ϕ₀, bc = NeumannBC(), integrator)\n    integrate!(eq, 2π)\n    V = LevelSetMethods.volume(eq)\n    (; name, V, err = abs(V - V₀) / V₀)\nend\n\nerrs = Dict(r.name => r.err for r in results)                                                # hide\n@assert errs[\"ForwardEuler\"] > 4 * errs[\"RK2\"] \"ForwardEuler should lose several × more area than RK2\"  # hide\n@assert errs[\"ForwardEuler\"] > 4 * errs[\"RK3\"] \"ForwardEuler should lose several × more area than RK3\"  # hide\n@assert abs(errs[\"RK2\"] - errs[\"RK3\"]) < 0.1 * errs[\"RK2\"] \"RK2 and RK3 should be nearly indistinguishable\"  # hide\n\nio = IOBuffer()\nprintln(io, \"| Integrator | Final area | Area error |\")\nprintln(io, \"|:--|--:|--:|\")\nfor r in results\n    @printf(io, \"| %s | %.4f | %.2f%% |\\n\", r.name, r.V, 100 * r.err)\nend\nMarkdown.parse(String(take!(io)))\n\nThe first-order ForwardEuler loses several times more area than the two higher-order schemes. RK2 and RK3 are almost indistinguishable here: once the temporal error drops below the fifth-order WENO5 spatial error, refining the time integration further buys little. This is why RK2 — the default — is the right balance for most problems; it removes the first-order temporal error of ForwardEuler without the extra stage cost of RK3.","title":"Comparing the integrators"},{"category":"section","location":"time-integrators/#The-semi-implicit-scheme","page":"Time integration","text":"SemiImplicitI2OE is a finite-volume scheme designed specifically for advection. Being semi-implicit, it remains stable at much larger time steps than the explicit schemes — its cfl factor defaults to 2.0 — so it reaches a given final time in far fewer steps. On the rotation above, one revolution costs only 198 steps versus 792 for the explicit schemes at their default CFL, trading some per-step accuracy for a substantially lower step count.\n\nIt comes with restrictions: it requires a full-grid MeshField (not a narrow band) and exactly one AdvectionTerm. Using it is otherwise a one-keyword change:\n\neq = LevelSetEquation(;\n    terms      = AdvectionTerm(𝐮),\n    ic         = ϕ₀,\n    bc         = NeumannBC(),\n    integrator = SemiImplicitI2OE(),\n)\nintegrate!(eq, 2π)\nnothing # hide","title":"The semi-implicit scheme"},{"category":"section","location":"boundary-conditions/#boundary-conditions","page":"Boundary conditions","text":"Finite-difference and WENO stencils reach beyond the mesh near its borders; a boundary condition supplies the missing ghost values. The following are available:\n\nType Description\nPeriodicBC Periodic (wrap-around)\nSymmetryBC Symmetry plane (reflection; for axisymmetric problems)\nExtrapolationBC{P} P-th order one-sided polynomial extrapolation\nNeumannBC Alias for ExtrapolationBC{0} (constant extension, ∂ϕ/∂n = 0)\nLinearExtrapolationBC Alias for ExtrapolationBC{1} (linear extrapolation, ∂²ϕ/∂n² = 0)\n\nExtrapolationBC{P} uses the P+1 nearest interior cells to build a degree-P polynomial and extrapolates it into the ghost region; higher P gives smoother outflow at the cost of a wider stencil.","title":"Boundary conditions"},{"category":"section","location":"boundary-conditions/#Specifying-conditions-per-face","page":"Boundary conditions","text":"When constructing a level-set equation you can pass up to 2d boundary conditions, one per face, where d is the dimension. The convention is:\n\na single boundary condition is applied to all 2d faces;\na length-d collection bcs assigns bcs[i] to direction i, and there bcs[i] may be\na single condition, applied to both faces of direction i, or\na tuple (lo, hi) of two conditions, applied to the lower/left and upper/right face.","title":"Specifying conditions per face"},{"category":"section","location":"boundary-conditions/#Periodic-vs.-open-boundaries","page":"Boundary conditions","text":"The clearest way to see what a condition does is to watch the same flow leave the domain under different boundaries. We advect a disk to the right with boldsymbol u = (1 0): a PeriodicBC wraps it back in from the left, while a NeumannBC lets it flow out cleanly.\n\nusing LevelSetMethods, GLMakie\nLevelSetMethods.set_makie_theme!()\ngrid = CartesianGrid((-1, -1), (1, 1), (48, 48))\nϕ₀ = MeshField(x -> sqrt(x[1]^2 + x[2]^2) - 0.5, grid)\n𝐮 = AdvectionTerm((x, t) -> (1, 0))\n\neq_periodic = LevelSetEquation(; ic = ϕ₀, bc = PeriodicBC(), terms = 𝐮)\neq_open     = LevelSetEquation(; ic = ϕ₀, bc = NeumannBC(),  terms = 𝐮)\n\nfig = Figure(; size = (1000, 480))\nfor (n, t) in enumerate((0.0, 0.5, 0.75, 1.0))\n    integrate!(eq_periodic, t)\n    integrate!(eq_open, t)\n    ax1 = Axis(fig[1, n]; title = \"periodic, t = $t\")\n    plot!(ax1, eq_periodic)\n    ax2 = Axis(fig[2, n]; title = \"open (Neumann), t = $t\")\n    plot!(ax2, eq_open)\nend\nfig\n\nFor higher-order outflow, use ExtrapolationBC{P} directly: ExtrapolationBC(5), say, fits a degree-5 polynomial through the 6 nearest interior cells. Higher P smooths the extrapolation into the ghost region at the cost of a wider stencil; for plain outflow like the above it behaves much like NeumannBC.","title":"Periodic vs. open boundaries"},{"category":"section","location":"boundary-conditions/#Mixing-conditions","page":"Boundary conditions","text":"Pass a per-direction tuple to combine conditions — here open in x, periodic in y, under a diagonal flow boldsymbol u = (1 1):\n\nbc = (NeumannBC(), PeriodicBC())   # Neumann in x, periodic in y\neq = LevelSetEquation(; ic = ϕ₀, bc, terms = AdvectionTerm((x, t) -> (1, 1)))\nfig = Figure(; size = (1000, 280))\nfor (n, t) in enumerate((0.0, 0.5, 0.75, 1.0))\n    integrate!(eq, t)\n    ax = Axis(fig[1, n]; title = \"t = $t\")\n    plot!(ax, eq)\nend\nfig","title":"Mixing conditions"},{"category":"section","location":"boundary-conditions/#Symmetry-planes","page":"Boundary conditions","text":"A SymmetryBC treats a boundary as a mirror: the field is reflected across it, so the interface meets the boundary perpendicularly. Like NeumannBC it enforces partialphipartial n = 0, but by reflection rather than flat extension — exactly the condition that holds on the axis of a symmetric problem. That lets you simulate only half of such a problem and recover the rest by reflection.\n\nBelow we stretch a disk centred on the axis x = 0 by the symmetric strain flow boldsymbolu = (x 0), solving it twice: on the full domain, and on the right half with a SymmetryBC on the axis. We overlay the two — full solution solid, half solution a dashed red contour, axis x = 0 dotted. The recipe forwards standard Makie attributes (color, linestyle, linewidth, and a fill toggle for the interior shading), which is what lets the half solution be drawn as a bare contour on top of the full one:\n\n𝐯 = (x, t) -> (x[1], 0.0)   # symmetric about x = 0 (uₓ is odd in x)\nic(g) = MeshField(x -> hypot(x...) - 0.4, g)\n\ngrid_full = CartesianGrid((-1.0, -1.0), (1.0, 1.0), (40, 40))\ngrid_half = CartesianGrid(( 0.0, -1.0), (1.0, 1.0), (20, 40))   # right half only\neq_full = LevelSetEquation(; ic = ic(grid_full), bc = NeumannBC(), terms = AdvectionTerm(𝐯))\n# SymmetryBC on the axis face (x = 0) only; Neumann on the remaining faces\neq_half = LevelSetEquation(; ic = ic(grid_half), bc = ((SymmetryBC(), NeumannBC()), NeumannBC()), terms = AdvectionTerm(𝐯))\n\nfig = Figure(; size = (1000, 340))\nfor (n, t) in enumerate((0.0, 0.3, 0.6))\n    integrate!(eq_full, t)\n    integrate!(eq_half, t)\n    ax = Axis(fig[1, n]; title = \"t = $t\")\n    plot!(ax, eq_full; color = :crimson, linestyle = :dash)\n    plot!(ax, eq_half; fill = false, showgrid = false)\n    vlines!(ax, [0.0]; color = (:black, 0.4), linestyle = :dot)     # the symmetry axis\nend\nfig\n\nAt every instant the dashed half-domain interface lies on the right half of the solid full-domain one: the SymmetryBC reproduces the symmetric solution from half the domain — the missing left half being just its mirror image — at half the computational cost.\n\nFor the precise stencil each condition applies, see the docstring of the corresponding type.","title":"Symmetry planes"},{"category":"section","location":"terms/#terms","page":"Level-set terms","text":"A level-set equation is given by\n\n  phi_t + sum_n textttterm_n = 0\n\nwhere each textttterm_n is a LevelSetTerm object. The following terms are available:\n\nusing LevelSetMethods\nusing InteractiveUtils # hide\nsubtypes(LevelSetMethods.LevelSetTerm)\n\nWe look at each in turn. Every example below evolves an equation and tiles a few snapshots side by side, so we factor that loop into a small helper:\n\nusing LevelSetMethods, StaticArrays, GLMakie\nLevelSetMethods.set_makie_theme!()\n\n# advance `eq` through `times` (increasing) and tile the states side by side\nfunction snapshots(eq, times; size = (1000, 280))\n    fig = Figure(; size)\n    for (n, t) in enumerate(times)\n        integrate!(eq, t)\n        ax = Axis(fig[1, n]; title = \"t = $t\")\n        plot!(ax, eq)\n    end\n    return fig\nend\nnothing # hide","title":"Level-set terms"},{"category":"section","location":"terms/#advection","page":"Level-set terms","text":"The simplest term is the advection term,\n\n  mathbfu cdot nabla phi\n\nwhere mathbfu is a velocity field. It models transport of the level-set by an external velocity field (see [1, Chapter 3]). Passing a MeshField to the AdvectionTerm constructor gives a velocity sampled at the grid points — useful when the field is time-independent or only known at nodes:\n\ngrid = CartesianGrid((-1, -1), (1, 1), (64, 64))\n𝐮 = MeshField(x -> SVector(1, 0), grid)\nϕ₀ = MeshField(x -> sqrt(x[1]^2 + x[2]^2) - 0.5, grid)\neq = LevelSetEquation(; terms = (AdvectionTerm(𝐮),), ic = ϕ₀, bc = NeumannBC())\nsnapshots(eq, [0.0, 0.5, 0.75, 1.0])\n\nThe level-set is advected to the right. For a time-dependent field, pass a function (x, t) instead — it receives the spatial coordinates x (an abstract vector of length d) and the time t, and must return a vector of length d:\n\neq = LevelSetEquation(; terms = AdvectionTerm((x, t) -> SVector(x[1]^2, 0)), ic = ϕ₀, bc = NeumannBC())\nsnapshots(eq, [0.0, 0.5, 0.75, 1.0])\n\nThe constructor also accepts a scheme as a second argument:\n\nUpwind(): first-order upwind scheme\nWENO5(): fifth-order WENO scheme (default)\n\nThe WENO scheme is more expensive but much more accurate, and is usually preferable to the upwind scheme, which introduces significant numerical diffusion. Comparing the two on a purely rotational velocity field acting on a dumbbell (assembled with the CSG operators from the geometry page) makes the difference plain:\n\ndisk(c) = MeshField(x -> hypot((x .- c)...) - 0.25, grid)\nbar = MeshField(x -> maximum(abs.(x) .- (1.0, 0.2) ./ 2), grid)\nϕ₀ = disk((-0.5, 0.0)) ∪ disk((0.5, 0.0)) ∪ bar # a dumbbell\n𝐮  = MeshField(grid) do (x, y)\n    SVector(-y, x)\nend\neq_upwind = LevelSetEquation(; terms = AdvectionTerm(𝐮, Upwind()), ic = ϕ₀, bc = NeumannBC())\neq_weno   = LevelSetEquation(; terms = AdvectionTerm(𝐮), ic = ϕ₀, bc = NeumannBC())\nfig = Figure(size = (1000, 400))\nplot!(Axis(fig[1, 1]; title = \"Initial\"), eq_upwind)\nintegrate!(eq_upwind, π) # half a revolution\nintegrate!(eq_weno, π)\nplot!(Axis(fig[1, 2]; title = \"Upwind (final time)\"), eq_upwind)\nplot!(Axis(fig[1, 3]; title = \"WENO5 (final time)\"), eq_weno)\nfig","title":"Advection"},{"category":"section","location":"terms/#normal-motion","page":"Level-set terms","text":"The normal motion term is\n\n  v nabla phi\n\nwhere v is a scalar field. It moves the level-set in the normal direction (see [1, Chapter 6]). Here it is on a star-shaped interface (see the geometry page):\n\ngrid = CartesianGrid((-2, -2), (2, 2), (64, 64))\nϕ = MeshField(grid) do x # a star\n    r, θ = hypot(x...), atan(x[2], x[1])\n    return r - (1 + 0.25 * cos(5θ))\nend\neq = LevelSetEquation(; terms = (NormalMotionTerm((x, t) -> 0.5),), ic = ϕ, bc = NeumannBC())\nsnapshots(eq, [0.0, 0.5, 0.75, 1.0])\n\nAs with AdvectionTerm, you can provide an update callback to mutate a mesh-based speed field before each stage of time integration:\n\nvfield = MeshField(x -> 0.0, grid)\nNormalMotionTerm(vfield, (v, ϕ, t) -> (values(v) .= 0.25 + 0.1 * t))\n\nIn Stefan problems, the speed v may only be known near the interface ϕ = 0. You can extend that interface speed to a band around the interface with extend_along_normals! (see velocity extension), then pass the result to NormalMotionTerm:\n\nv = zeros(Float64, size(grid)...)\nΔ = minimum(LevelSetMethods.meshsize(grid))\nfrozen = abs.(values(ϕ)) .<= 1.5Δ\nfor I in CartesianIndices(v)\n    frozen[I] || continue\n    x = getnode(grid, I)\n    v[I] = 0.2 + 0.1 * cos(2π * atan(x[2], x[1]))\nend\nextend_along_normals!(v, ϕ; frozen, nb_iters = 80)\nNormalMotionTerm(MeshField(v, grid))","title":"Normal motion"},{"category":"section","location":"terms/#curvature","page":"Level-set terms","text":"This term moves the level-set in the normal direction with a velocity proportional to the mean curvature,\n\n  b kappa nabla phi\n\nwhere kappa = nabla cdot (nabla phi  nabla phi). The coefficient b should be negative; a positive value yields an ill-posed evolution (akin to a negative diffusion coefficient). Here is the classic motion by mean curvature on a spiral-like level-set:\n\ngrid = CartesianGrid((-1, -1), (1, 1), (64, 64))\n# a spiral level-set\nd, r0, θ0, α = 1, 0.5, -π / 3, π / 100.0\nR = [cos(α) -sin(α); sin(α) cos(α)]\nM = R * [1/0.06^2 0; 0 1/(4π^2)] * R'\nϕ = MeshField(grid) do (x, y)\n    r, θ = sqrt(x^2 + y^2), atan(y, x)\n    result = 1e30\n    for i in 0:4\n        v = [r - r0; (θ + (2i - 4) * π) - θ0]\n        result = min(result, sqrt(v' * M * v) - d)\n    end\n    return result\nend\neq = LevelSetEquation(; terms = (CurvatureTerm((x, t) -> -0.1),), ic = ϕ, bc = NeumannBC())\nsnapshots(eq, [0.0, 0.1, 0.2, 0.3])","title":"Curvature motion"},{"category":"section","location":"terms/#reinitialization","page":"Level-set terms","text":"The reinitialization term evolves\n\n  phi_t + textsign(phi) left( nabla phi - 1 right) = 0\n\nto keep the level-set function close to a signed distance function — sometimes important for numerical stability. The evolution penalizes deviation from nabla phi = 1 without moving the zero contour; in practice a smeared sign is used (see [1, Chapter 7]). Starting from a circle level-set that is not a signed distance, the term drives it toward the true SDF:\n\ngrid = CartesianGrid((-1, -1), (1, 1), (64, 64))\nϕ = MeshField(x -> x[1]^2 + x[2]^2 - 0.5^2, grid)        # circle, but not a signed distance\neq = LevelSetEquation(; terms = (EikonalReinitializationTerm(),), ic = ϕ, bc = NeumannBC())\nfig = Figure(; size = (1000, 280))\nfor (n, t) in enumerate([0.0, 0.25, 0.5, 0.75])\n    integrate!(eq, t)\n    ax = Axis(fig[1, n]; title = \"t = $t\")\n    contour!(ax, current_state(eq); levels = [0.25, 0, 0.5], labels = true, labelsize = 14)\nend\nfig\n\nAs the equation evolves, the evenly spaced contours of ϕ relax toward those of the signed distance function sqrtx^2 + y^2 - 05.\n\nAlternatively, applying the sign to the initial level-set only,\n\n  phi_t + textsign(phi_0) left( nabla phi - 1 right) = 0\n\nis enabled by passing a MeshField to the constructor — EikonalReinitializationTerm(ϕ) — and yields a closely matching result.\n\ntip: Consider Newton reinitialization instead\nEikonalReinitializationTerm requires many pseudo-time steps to propagate corrections away from the interface and can cause spurious mass loss. For most use cases, reinitialize! (Newton closest-point) is a better choice: it samples the interface, builds a KD-tree, and computes the signed distance to high order in a single pass. See the Signed distance functions page for details.","title":"Reinitialization term"},{"category":"section","location":"extension-mmg/#extension-mmg","page":"MMG extension","text":"Loading MMG_jll (with MarchingCubes for surface meshes) activates two functions that turn a level set into an unstructured mesh via MMG: LevelSetMethods.export_volume_mesh meshes the interior phi  0, and LevelSetMethods.export_surface_mesh meshes the interface phi = 0. Both accept optional parameters controlling element sizing:\n\nhgrad — the growth ratio allowed between two adjacent edges.\nhmin / hmax — lower and upper bounds on the edge sizes.\nhausd — the maximal distance between the piecewise-linear mesh and the reconstructed ideal boundary.\n\nEach function writes a .mesh file to disk. To see the result we render it with Gmsh; since the rendering boilerplate is the same every time, we wrap it in a small helper that opens a mesh, applies a few display options, and writes a PNG offscreen:\n\nusing LevelSetMethods, MMG_jll, MarchingCubes, Gmsh, GLMakie\n\n# render an MMG `.mesh` file to `pngfile`; `options` go straight to `gmsh.option.setNumber`,\n# and `hide` lists `(dim, tag)` entities (e.g. a subdomain) to make invisible\nfunction render_mesh(meshfile, pngfile; options = (), hide = ())\n    GLMakie.closeall()          # release any GL context before Gmsh's FLTK\n    gmsh.initialize()\n    try\n        gmsh.option.setNumber(\"General.Verbosity\", 0)\n        gmsh.open(meshfile)\n        isempty(hide) || gmsh.model.setVisibility(collect(hide), 0)\n        for (name, val) in options\n            gmsh.option.setNumber(name, val)\n        end\n        gmsh.fltk.initialize()\n        gmsh.write(pngfile)\n        gmsh.fltk.finalize()\n    finally\n        gmsh.finalize()\n    end\n    return pngfile\nend\nnothing # hide","title":"MMG extension"},{"category":"section","location":"extension-mmg/#Volume-meshes","page":"MMG extension","text":"LevelSetMethods.export_volume_mesh meshes the enclosed region of a 2D or 3D level set (via the mmg2d_O3 / mmg3d_O3 utilities). In 2D:\n\ngrid = CartesianGrid((-2, -2), (2, 2), (50, 50))\nϕ = MeshField(grid) do x   # a star; see the geometry page\n    r, θ = hypot(x...), atan(x[2], x[1])\n    return r - (1 + 0.25 * cos(5θ))\nend\nvolume_mesh2d = LevelSetMethods.export_volume_mesh(ϕ, joinpath(@__DIR__, \"volume2D.mesh\"))\n\nrender_mesh(volume_mesh2d, joinpath(@__DIR__, \"volume2d.png\"))\nnothing # hide\n\n(Image: Volume2D)\n\nThe 3D case is identical — pass a 3D level set; here hausd/hmax refine the mesh around the interface:\n\ngrid = CartesianGrid((-1, -1, -1), (+1, +1, +1), (40, 40, 40))\nϕ = MeshField(x -> hypot(x...) - 0.5, grid)   # a sphere; see the geometry page\nvolume_mesh3d = LevelSetMethods.export_volume_mesh(ϕ, joinpath(@__DIR__, \"volume3d.mesh\"); hausd = 0.02, hmax = 0.15)\n\nMMG's level-set meshing splits the background box into two subdomains — the enclosed region phi  0 (reference 3) and the exterior phi  0 (reference 2) — which Gmsh loads as two elementary volumes. The exterior fills the whole box and hides everything inside it, so we make it invisible (hide = ((3, 2),)) and keep only the ball. Clipping the remaining mesh through x = 0 with ClipWholeElements then cuts away whole tetrahedra to expose the interior:\n\nrender_mesh(volume_mesh3d, joinpath(@__DIR__, \"volume3d.png\");\n    options = (\"General.Clip0A\" => 1.0, \"General.Clip0D\" => 0.0,   # clip plane x = 0\n               \"General.ClipWholeElements\" => 1, \"Mesh.Clip\" => 1,\n               \"Mesh.VolumeEdges\" => 1, \"Mesh.VolumeFaces\" => 1,\n               \"Mesh.SurfaceEdges\" => 0, \"Mesh.SurfaceFaces\" => 0,\n               \"General.RotationX\" => 300, \"General.RotationZ\" => 160, \"General.Trackball\" => 0))\nnothing # hide\n\n(Image: Volume3D)\n\nThe smooth left side is the tetrahedra's outer faces conforming to the spherical interface; the jagged right side is the cutaway, revealing the tetrahedra that fill the enclosed region.","title":"Volume meshes"},{"category":"section","location":"extension-mmg/#Surface-meshes","page":"MMG extension","text":"To mesh the interface itself rather than the interior, use LevelSetMethods.export_surface_mesh, which combines MarchingCubes.jl with MMG's mmgs_O3 surface remesher:\n\ngrid = CartesianGrid((-2, -1, -1), (+2, +1, +1), (80, 40, 40))\nϕ₁ = MeshField(x -> hypot((x .- (-1, 0, 0))...) - 0.5, grid)\nϕ₂ = MeshField(x -> hypot((x .- (+1, 0, 0))...) - 0.5, grid)\nϕ₃ = MeshField(x -> maximum(abs.(x) .- (2, 0.25, 0.25) ./ 2), grid)\nϕ  = ϕ₁ ∪ ϕ₂ ∪ ϕ₃   # two spheres joined by a bar; see the geometry page\nsurf_mesh3d = LevelSetMethods.export_surface_mesh(ϕ, joinpath(@__DIR__, \"surface3D.mesh\"); hausd = 0.01, hmax = 0.1, hgrad = 1.3)\n\nrender_mesh(surf_mesh3d, joinpath(@__DIR__, \"surface3d.png\");\n    options = (\"Mesh.SurfaceFaces\" => 1, \"Mesh.SurfaceEdges\" => 1,\n               \"General.RotationX\" => 300, \"General.RotationZ\" => 300, \"General.Trackball\" => 0))\nnothing # hide\n\n(Image: Surface3D)","title":"Surface meshes"},{"category":"section","location":"levelset-equation/#levelset-equation","page":"Level-set equation","text":"The LevelSetEquation is the central object of the package. It bundles everything needed to evolve an interface — the current state phi, the terms that move it, the time integrator, and the boundary conditions — into a single value that you advance in time with integrate!. The equation has the form\n\nphi_t + sum_n textttterm_n = 0 \n\nwhere each textttterm_n is a LevelSetTerm. This page covers how to build an equation, inspect it, and step it forward; the individual terms, integrators, and boundary conditions each have their own chapter.","title":"Level-set equation"},{"category":"section","location":"levelset-equation/#Building-an-equation","page":"Level-set equation","text":"A LevelSetEquation is constructed with keyword arguments:\n\nusing LevelSetMethods\ngrid = CartesianGrid((-1, -1), (1, 1), (32, 32))\nϕ₀ = MeshField(x -> hypot(x...) - 0.5, grid)   # initial level set: a circle\neq = LevelSetEquation(;\n    terms = AdvectionTerm((x, t) -> (-x[2], x[1])), # rotate about the origin\n    ic = ϕ₀, # initial condition (copied internally; the original is left untouched)\n    bc = NeumannBC(), # boundary conditions\n    integrator = RK2(), # time integrator (default)\n    t = 0.0, # initial time (default)\n)\n\nThe keywords are:\n\nterms — a LevelSetTerm or a tuple of them. With several terms the equation is their sum; the displayed equation reflects this. See Level-set terms.\nic — the initial condition, any mesh field. A dense MeshField gives a full-grid computation; a NarrowBandMeshField gives a narrow-band one (see Narrow-band fields). It is copied on construction, so your original field is left untouched; the evolving state lives in current_state.\nbc — the boundary conditions. They may instead be carried by ic (built with the bc keyword); supplying both warns and the equation's bc wins. A single condition applies to every face; a per-dimension tuple applies different ones.\nintegrator — the time integrator; defaults to RK2.\nt — the initial time; defaults to 0.\n\nPassing several terms sums them, and the displayed equation updates accordingly:\n\nLevelSetEquation(;\n    terms = (AdvectionTerm((x, t) -> (-x[2], x[1])), CurvatureTerm((x, t) -> -0.01)),\n    ic    = ϕ₀,\n    bc    = NeumannBC(),\n)","title":"Building an equation"},{"category":"section","location":"levelset-equation/#Inspecting-the-state","page":"Level-set equation","text":"The equation exposes its parts through accessors. current_time returns the current time, and current_state the mesh field holding phi at that time:\n\ncurrent_state(eq)\n\nThe state is a live mesh field: you can index it, compute geometric quantities on it (see Geometric quantities), or pass it to reinitialize!. The underlying CartesianGrid, the tuple of terms, the integrator, and the boundary conditions are likewise available with LevelSetMethods.mesh, LevelSetMethods.terms, LevelSetMethods.time_integrator, and LevelSetMethods.boundary_conditions.","title":"Inspecting the state"},{"category":"section","location":"levelset-equation/#Advancing-in-time","page":"Level-set equation","text":"integrate! evolves the equation up to a final time tf, mutating current_state(eq) and current_time(eq) in place:\n\nintegrate!(eq, 0.5)\n@assert current_time(eq) == 0.5 # hide\ncurrent_time(eq)\n\nThe internal time step is chosen automatically from a CFL condition that depends on the terms and the integrator, so you only specify where to stop, not how. An optional third argument caps the step from above — useful when an external process (a hook, a coupled solver) must see the solution at least that often:\n\nintegrate!(eq, 1.0, 0.01)   # continue to t = 1, with no internal step exceeding Δt = 0.01\n@assert current_time(eq) == 1.0 # hide\ncurrent_time(eq)\n\nBecause integrate! is incremental, calling it repeatedly at increasing times steps the same equation forward — the idiom behind animations and the snapshots below:\n\nusing CairoMakie\nCairoMakie.activate!()\nLevelSetMethods.set_makie_theme!()\neq = LevelSetEquation(; terms = AdvectionTerm((x, t) -> (-x[2], x[1])),\n                         ic = MeshField(x -> maximum(abs.(x) .- (0.7, 0.3)), grid),\n                         bc = NeumannBC())\nfig = Figure(; size = (900, 250))\nfor (n, t) in enumerate((0.0, π / 4, π / 2, 3π / 4))\n    integrate!(eq, t)\n    ax = Axis(fig[1, n]; title = \"t = $(round(t; digits = 2))\")\n    plot!(ax, eq)\nend\nfig","title":"Advancing in time"},{"category":"section","location":"levelset-equation/#Hooks:-customizing-each-step","page":"Level-set equation","text":"Two optional callbacks let you run code around each accepted step (as opposed to each internal Runge–Kutta stage): prehook(eq) runs at the start of a step, before the state is advanced, and posthook(eq) runs after the step has been committed. Both receive the equation with its state and time synced to that point, and both may mutate the state. Their return values are ignored.\n\nThe most important use is reinitialization. Keeping phi a signed distance function is not built into the integrator; you drive it from a posthook (see Reinitialization):\n\neq = LevelSetEquation(; terms = AdvectionTerm((x, t) -> (-x[2], x[1])), ic = ϕ₀, bc = NeumannBC())\nintegrate!(eq, π / 2; posthook = e -> reinitialize!(current_state(e)))\nnothing # hide\n\nBecause the hook is an ordinary function, you control when it fires — every step, or gated on a step counter, the elapsed current_time(e), or a measured drift of nablaphi from one. A posthook is also the natural place for diagnostics. Here we record the enclosed volume after every step and plot its drift from the initial value — a direct measure of how well this divergence-free rotation conserves mass:\n\neq = LevelSetEquation(; terms = AdvectionTerm((x, t) -> (-x[2], x[1])), ic = ϕ₀, bc = NeumannBC())\nV₀ = LevelSetMethods.volume(current_state(eq))\ntimes, errs = Float64[], Float64[]\nintegrate!(eq, 2π; posthook = e -> begin\n    push!(times, current_time(e))\n    push!(errs, abs(LevelSetMethods.volume(current_state(e)) - V₀) / V₀)\nend)\nfig = Figure(; size = (600, 300))\nax = Axis(fig[1, 1]; xlabel = \"t\", ylabel = \"|V - V₀| / V₀\", title = \"Volume drift\")\nlines!(ax, times, errs)\nfig","title":"Hooks: customizing each step"},{"category":"section","location":"levelset-equation/#Where-to-go-next","page":"Level-set equation","text":"Each ingredient of the equation has a dedicated chapter:\n\nLevel-set terms — the building blocks of the right-hand side and how to customize them.\nTime integration — the available integrators and their trade-offs.\nBoundary conditions — the conditions and how to set them per face.\nSigned distance functions — keeping phi well-behaved with reinitialize!, the main use of posthook.","title":"Where to go next"},{"category":"section","location":"grids/#grids","page":"Grids and mesh fields","text":"Every computation in LevelSetMethods.jl rests on two ingredients: a mesh that discretizes the domain, and data attached to that mesh. This page covers both — the CartesianGrid mesh and the AbstractMeshField data built on it. The next page, Level sets, focuses on the real-valued fields the level-set method actually evolves.","title":"Grids and mesh fields"},{"category":"section","location":"grids/#The-Cartesian-mesh","page":"Grids and mesh fields","text":"A CartesianGrid is a uniform, axis-aligned mesh covering an N-dimensional box lc hc, described by its lower corner lc, upper corner hc, and node count n per direction. The dimension N is inferred from the corners, so the same constructor builds meshes in any number of dimensions:\n\nusing LevelSetMethods\ngrid = CartesianGrid((-1, -1), (1, 1), (64, 64))\n\nTo specify a target resolution rather than a node count, pass meshsize instead; the domain is honored exactly and the cell count rounded up, so the spacing is never coarser than requested:\n\nCartesianGrid((0, 0), (1, 1); meshsize = 0.3)","title":"The Cartesian mesh"},{"category":"section","location":"grids/#Nodes-and-cells","page":"Grids and mesh fields","text":"A Cartesian mesh has two kinds of geometric entities, each addressed by a CartesianIndex:\n\nNodes are the grid points, indexed 1:n[d] in each dimension d. Field values live here.\nCells are the hyperrectangles between neighbouring nodes; cell I spans node I to node I + 1, giving n[d] - 1 cells per dimension.\n\nIterate over them with nodeindices / cellindices, recover the geometry of an index with getnode / getcell, and query the mesh with size, ndims, and LevelSetMethods.meshsize:\n\ngetnode(grid, 1, 1), size(grid), LevelSetMethods.meshsize(grid)","title":"Nodes and cells"},{"category":"section","location":"grids/#Attaching-data:-mesh-fields","page":"Grids and mesh fields","text":"A mesh on its own carries no data. To represent a quantity — a level-set function, a velocity, a speed — you attach a value to each node, producing a mesh field. All mesh fields are subtypes of AbstractMeshField and share one interface (index by CartesianIndex, query the underlying mesh, iterate over the nodes), so code written against that interface works regardless of how the values are stored. Two storage strategies are provided, differing only in where the values are kept: a dense MeshField stores a value at every node, while a sparse NarrowBandMeshField stores values only on a thin band of nodes around the interface.","title":"Attaching data: mesh fields"},{"category":"section","location":"grids/#Dense-fields","page":"Grids and mesh fields","text":"The most direct way to build a MeshField is to sample a function at each node:\n\nϕ = MeshField(x -> x[1]^2 + x[2]^2 - 0.5^2, grid)\n\nThe function receives the node coordinates (as an SVector) and may return any value type — a scalar for a level set, an SVector for a velocity field, and so on. Values are read and written by Cartesian index, exactly like an array (ϕ[3, 3]). You can also build a field directly from an array of values, which is handy when the data comes from elsewhere:\n\nusing LinearAlgebra\nvals = [norm(getnode(grid, I)) - 0.5 for I in nodeindices(grid)]\nMeshField(vals, grid)","title":"Dense fields"},{"category":"section","location":"grids/#Sparse-fields","page":"Grids and mesh fields","text":"When the interface fills only a thin region of the domain, storing phi at every node is wasteful. A NarrowBandMeshField keeps values only on a band of nodes around the interface while presenting the very same interface as a dense field — most easily built by restricting an existing MeshField to a band:\n\nnb = NarrowBandMeshField(ϕ; nlayers = 3)\n@assert 300 < length(active_nodeindices(nb)) < 1000 # hide\nnb\n\nThe display reports how many nodes are active (stored in the band) out of the whole mesh. The two storage strategies are reconciled by a single distinction in the interface: nodeindices / cellindices range over the entire mesh, while active_nodeindices / active_cellindices range over the subset a field actually stores. For a dense MeshField the two coincide; for a NarrowBandMeshField the active set is just the band. Code that should work for either storage iterates over the active set — the idiom used throughout the package:\n\nsum(I -> nb[I], active_nodeindices(nb))   # works for dense and narrow-band fields alike\n\nSee Narrow-band fields for when and how to use the band in detail.","title":"Sparse fields"},{"category":"section","location":"grids/#Boundary-conditions","page":"Grids and mesh fields","text":"Finite-difference and WENO stencils reach beyond the mesh near its borders. To resolve those out-of-grid reads, any mesh field can carry boundary conditions via the bc keyword; indexing outside the mesh is then well defined, with the condition supplying the ghost value:\n\nψ = MeshField(x -> x[1]^2 + x[2]^2 - 0.5^2, grid; bc = NeumannBC())\nψ[0, 3]   # one node past the left border, filled by the Neumann condition\n\nA field without boundary conditions throws on an out-of-grid index rather than silently guessing. See Boundary conditions for the full list of available conditions and how to apply different ones per face.","title":"Boundary conditions"},{"category":"section","location":"grids/#Real-valued-fields-are-level-sets","page":"Grids and mesh fields","text":"There is no separate \"level set\" type in this package. A mesh field whose values are real numbers is a level set, by the convention that its zero contour x  phi(x) = 0 represents the interface, with phi  0 inside and phi  0 outside. Fields with non-real values (such as an SVector-valued velocity) are ordinary mesh fields too; they simply are not interpreted as level sets. The next page, Level sets, is devoted to building real-valued fields and combining them into geometries.","title":"Real-valued fields are level sets"},{"category":"section","location":"interpolation/#interpolation","page":"Interpolation","text":"A AbstractMeshField stores values at grid nodes and nothing in between. An InterpolatedField equips it with piecewise-polynomial interpolation, turning it into a continuous, callable function you can evaluate — and differentiate — anywhere in the domain. It is also the engine underneath reinitialization, NewtonSDF, and the ImplicitIntegration quadrature, though most users meet it through the off-grid queries below.","title":"Interpolation"},{"category":"section","location":"interpolation/#Building-an-interpolant","page":"Interpolation","text":"Wrap a AbstractMeshField in an InterpolatedField, choosing the polynomial order. The result is callable and evaluates the local interpolant at the query point — coordinates may be passed as separate scalars or as a single point-like object such as an SVector:\n\nusing LevelSetMethods\nstar = MeshField(CartesianGrid((-2.0, -2.0), (2.0, 2.0), (64, 64))) do x   # see the geometry page\n    r, θ = hypot(x...), atan(x[2], x[1])\n    return r - (1 + 0.25 * cos(5θ))\nend\nϕ = InterpolatedField(star, 3)\nϕ(0.5, 0.5)   # evaluate at an arbitrary point inside the grid\n\nThe construction is identical in any dimension.","title":"Building an interpolant"},{"category":"section","location":"interpolation/#Derivatives","page":"Interpolation","text":"The interpolant differentiates its local polynomial patch exactly (via ForwardDiff), so LevelSetMethods.gradient and LevelSetMethods.hessian return analytic derivatives rather than further finite-difference approximations. When you need the value alongside its derivatives, the fused value_and_gradient and value_gradient_hessian compute them together in a single pass:\n\nusing StaticArrays\nval, grad = LevelSetMethods.value_and_gradient(ϕ, SVector(0.1, 0.2))\n\nnote: Thread safety\nAn InterpolatedField is safe to evaluate concurrently from multiple tasks: each task gets its own scratch buffer and the shared interpolation operator is read-only. Mutating the field (setindex!, copy!) while other tasks evaluate it is not safe; mutations invalidate every task's cached cell coefficients.","title":"Derivatives"},{"category":"section","location":"signed-distance/#signed-distance","page":"Closest-point reinitialization","text":"A signed distance function (SDF) is a level set with the extra property nablaphi = 1: phi(x) is the distance from x to the interface, negative inside and positive outside. Curvature, normals, and the width of a narrow band all behave best when phi is (close to) an SDF, but advection and other terms steadily distort it. Reinitialization restores the signed-distance property while leaving the zero level set in place.\n\nThis package offers two routes:\n\nreinitialize! (this page, recommended): a geometric method that samples the interface, builds a KD-tree, and computes the signed distance to high order with a Newton closest-point solve, following Saye [4]. It converges in a single pass.\nEikonalReinitializationTerm: a PDE-based alternative that evolves phi_t + operatornamesign(phi)(nablaphi - 1) = 0 using the ordinary time-stepping machinery. It is documented with the other level-set terms.","title":"Closest-point reinitialization"},{"category":"section","location":"signed-distance/#Reinitializing-a-level-set","page":"Closest-point reinitialization","text":"Call reinitialize! on a MeshField to restore the signed-distance property in place. Below, phi = x^2 + y^2 - r^2 describes the right circle but is not a distance function — its slope grows with radius — and reinitialization turns it into one without moving the zero contour:\n\nusing LevelSetMethods\nusing CairoMakie\nusing StaticArrays\n\ngrid = CartesianGrid((-1, -1), (1, 1), (64, 64))\nϕ = MeshField(x -> x[1]^2 + x[2]^2 - 0.5^2, grid; bc = LinearExtrapolationBC())\nbefore = InterpolatedField(copy(ϕ), 1)   # snapshot, to compare slices\nreinitialize!(ϕ)\nafter = InterpolatedField(ϕ, 1)\n\nxs = ys = range(-1, 1; length = 64)\nlevels = -0.2:0.2:0.8\nLevelSetMethods.set_makie_theme!()\nfig = Figure(; size = (820, 340))\naxc = Axis(fig[1, 1]; title = \"Reinitialized ϕ (evenly spaced ⇒ distance)\", aspect = 1,\n    xlabel = \"x\", ylabel = \"y\")\ncontour!(axc, xs, ys, values(ϕ); levels, labels = true)\ncontour!(axc, xs, ys, values(ϕ); levels = [0.0], color = :red, linewidth = 2)\naxs = Axis(fig[1, 2]; title = \"Before vs after\", xlabel = \"x  (slice at y = 0)\", ylabel = \"ϕ\")\nr = range(-0.95, 0.95; length = 200)\nlines!(axs, r, [before(SVector(x, 0.0)) for x in r]; label = \"before\", linewidth = 2)\nlines!(axs, r, [after(SVector(x, 0.0)) for x in r]; label = \"after (slope ±1)\", linewidth = 2)\nhlines!(axs, 0; color = :gray, linestyle = :dash)\naxislegend(axs; position = :ct)\nfig\n\nAfter reinitialization the contours are evenly spaced — the level phi = c sits a distance c from the interface — and the radial slice is a straight V of slope pm 1 instead of a parabola. The kink at the centre is genuine: an SDF is not differentiable on the medial axis (here, the single point equidistant from the whole circle).\n\nBoth curves cross zero at the same place, so the interface itself is preserved. Concretely, the reinitialized field matches the exact circle distance everywhere:\n\nexact(x) = hypot(x...) - 0.5\nmax_er = maximum(i -> abs(ϕ[i] - exact(getnode(grid, i))), eachindex(grid))\n@assert max_er < 1e-6 \"reinitialization should recover the signed distance function\"  # hide\nprintln(\"maximum error vs. exact distance: $max_er\")\n\nnote: The zero set moves a little\nDistances are measured to the interpolated interface, so the zero level set is preserved to the order of the interpolant, not exactly. This is far below the discretization error of the rest of the scheme, but it is not bit-for-bit invariance.","title":"Reinitializing a level set"},{"category":"section","location":"signed-distance/#Reinitializing-during-a-simulation","page":"Closest-point reinitialization","text":"Reinitialization is not built into the equation; it is driven from a posthook passed to integrate!, which runs after every accepted step. The simplest hook reinitializes on every step:\n\nintegrate!(eq, tf; posthook = eq -> reinitialize!(current_state(eq); upsample = 4))\n\nBecause the hook is an ordinary function, you decide when to reinitialize — on a step counter closed over by the hook, on the elapsed current_time(eq), or on a measured drift of nablaphi from one. Each call rebuilds the interface sampling, KD-tree, and per-node solve from scratch, so it is not free; reinitializing every few steps is usually enough to keep phi well-behaved.","title":"Reinitializing during a simulation"},{"category":"section","location":"signed-distance/#A-reusable-signed-distance-function","page":"Closest-point reinitialization","text":"Under the hood, reinitialize! builds a LevelSetMethods.NewtonSDF and writes its values back onto the grid. You can also keep that object and evaluate the signed distance at arbitrary points, without touching the level set — useful when an SDF is an ingredient in a larger computation (measuring clearances, seeding a velocity extension, querying off-grid):\n\nsdf = LevelSetMethods.NewtonSDF(ϕ; upsample = 2)\nd0 = sdf(SVector(0.0, 0.0))   # distance from the origin to the circle (≈ -0.5)\n@assert abs(d0 + 0.5) < 1e-3 \"the origin lies 0.5 inside the radius-0.5 circle\"  # hide\nd0\n\nThe interface sample points behind the KD-tree are available through LevelSetMethods.get_sample_points. Evaluating the object on a grid finer than the original shows that it really is a continuous distance field, anchored on those samples:\n\npts = LevelSetMethods.get_sample_points(sdf)\nfine = range(-1, 1; length = 120)\nD = [sdf(SVector(x, y)) for x in fine, y in fine]\n\nfig = Figure(; size = (430, 360))\nax = Axis(fig[1, 1]; aspect = 1, xlabel = \"x\", ylabel = \"y\",\n    title = \"Signed distance, evaluated anywhere\")\nhm = heatmap!(ax, fine, fine, D; colormap = :balance, colorrange = (-0.7, 0.7))\nsub = pts[1:6:end]\nscatter!(ax, first.(sub), last.(sub); color = :black, markersize = 5, label = \"interface samples\")\naxislegend(ax; position = :rt)\nColorbar(fig[1, 2], hm)\nfig\n\nnote: Thread safety\nNewtonSDF is safe to evaluate concurrently from multiple tasks: its interpolant keeps one scratch buffer per task, and the KD-tree and sample points are read-only during evaluation. This is what lets reinitialize! fill the grid in parallel.","title":"A reusable signed distance function"},{"category":"section","location":"signed-distance/#Accuracy-and-cost","page":"Closest-point reinitialization","text":"Both reinitialize! and NewtonSDF accept the same keywords, which trade accuracy against work:\n\nkeyword controls raise it when\norder polynomial degree of the local interpolant used for the closest-point solve you need higher-order accuracy near a curved interface\nupsample density of interface samples seeding the KD-tree the interface is under-resolved or finely featured\nmaxiters, xtol, ftol Newton solver stopping criteria the solver reports non-convergence\n\nBoth work on a full-grid MeshField and on a narrow-band field; in the latter case only the active nodes are reinitialized. If the solver fails to converge at some nodes — typically far from the interface, near the domain corners — reinitialize! warns and leaves a best-effort distance there.","title":"Accuracy and cost"},{"category":"section","location":"geometry/#geometry","page":"Level sets","text":"A level set is just a real-valued AbstractMeshField whose zero contour represents the interface of interest (see Grids and mesh fields). There is no separate \"shape\" type: you describe a geometry by the implicit function that vanishes on its boundary, sample it with e.g. MeshField(f, grid), and combine geometries with set operations. This page collects the common patterns.","title":"Level sets"},{"category":"section","location":"geometry/#Signed-distance-functions","page":"Level sets","text":"The most convenient implicit function is a signed distance function (SDF): ϕ(x) returns the distance from x to the interface, negative inside and positive outside. The same one-liner builds a circle in 2D or a sphere in 3D — only the grid's dimension changes:\n\nusing LevelSetMethods, LinearAlgebra\ngrid = CartesianGrid((-1, -1), (1, 1), (32, 32))\ncircle = MeshField(x -> norm(x) - 0.5, grid)   # ‖x‖ - r is a true SDF, in any dimension\n\nThe same function and grid can build a NarrowBandMeshField in one step, storing values only on a band of nodes around the interface rather than the whole grid (see Narrow-band fields):\n\ncircle_band = NarrowBandMeshField(x -> norm(x) - 0.5, grid; nlayers = 3)\n@assert 0 < length(active_nodeindices(circle_band)) < length(grid) # hide\ncircle_band","title":"Signed distance functions"},{"category":"section","location":"geometry/#Non-SDF-implicit-functions","page":"Level sets","text":"Any function with the correct sign works — it need not be a true distance. A box is easy to write, but is not a true SDF away from its faces:\n\nbox = MeshField(x -> maximum(abs.(x) .- (0.6, 0.3) ./ 2), grid)\n\nNear the corners the value is not the actual distance, so ‖∇ϕ‖ ≠ 1. If a downstream algorithm needs an SDF (e.g. a narrow band, or curvature near corners), reinitialize it with reinitialize!:\n\nreinitialize!(box)","title":"Non-SDF implicit functions"},{"category":"section","location":"geometry/#Combining-geometries-with-set-operations","page":"Level sets","text":"A level set denotes the domain Ω = x  ϕ(x)  0. Operations on those domains are represented by simple value combinations — union by min, intersection by max, set difference by max(ϕ₁, -ϕ₂) — and are available as ∪, ∩, setdiff (with in-place union!, intersect!, setdiff!).\n\nA dumbbell is the union of two disks and a connecting bar:\n\ndisk(c) = MeshField(x -> norm(x .- c) - 0.25, grid)\nbar = MeshField(x -> maximum(abs.(x) .- (1.0, 0.2) ./ 2), grid)\ndumbbell = disk((-0.5, 0.0)) ∪ disk((0.5, 0.0)) ∪ bar\n\nA Zalesak disk is a disk with a rectangular slot cut out:\n\nslot = MeshField(x -> maximum(abs.(x .- (0.0, -0.5)) .- (0.25, 1.0) ./ 2), grid)\nzalesak = setdiff(MeshField(x -> norm(x) - 0.5, grid), slot)\n\nnote: Note\nCombining SDFs with min/max keeps the sign correct everywhere but breaks the exact distance property near the seams. Reinitialize the result if you need a true SDF.","title":"Combining geometries with set operations"},{"category":"section","location":"geometry/#Parametric-interfaces","page":"Level sets","text":"Interfaces described in other coordinates work just as well. A star is convenient in polar form (this one is not an SDF):\n\nstar = MeshField(grid) do x\n    r, θ = norm(x), atan(x[2], x[1])\n    return r - 0.75 * (1 + 0.25 * cos(5θ))\nend","title":"Parametric interfaces"},{"category":"section","location":"geometry/#Visualizing-the-result","page":"Level sets","text":"Loading a Makie backend lets you inspect any of these level sets directly (see the Makie extension). The interior ϕ  0 is shaded and the zero contour drawn on top:\n\nusing CairoMakie\nLevelSetMethods.set_makie_theme!()\nfig = Figure(; size = (900, 280))\nfor (n, (name, ψ)) in enumerate((\"dumbbell\" => dumbbell, \"zalesak\" => zalesak, \"star\" => star))\n    ax = Axis(fig[1, n]; title = name)\n    plot!(ax, ψ)\nend\nfig","title":"Visualizing the result"},{"category":"section","location":"reference/#reference","page":"Reference","text":"","title":"Reference"},{"category":"section","location":"reference/#Bibliography","page":"Reference","text":"S. Osher and R. Fedkiw. Level Set Methods and Dynamic Implicit Surfaces. Vol. 153 (Springer New York, New York, NY, 2003).\n\n\n\nI. M. Mitchell and others. A toolbox of level set methods. UBC Department of Computer Science Technical Report TR-2007-11 1, 6 (2007).\n\n\n\nK. Mikula and M. Ohlberger. A new level set method for motion in normal direction based on a semi-implicit forward-backward diffusion approach. SIAM Journal on Scientific Computing 32, 1527–1544 (2010).\n\n\n\nR. Saye. High-order methods for computing distances to implicitly defined surfaces. Communications in Applied Mathematics and Computational Science 9, 107–141 (2014).\n\n\n\nD. Peng, B. Merriman, S. Osher, H. Zhao and M. Kang. A PDE-based fast local level set method. Journal of Computational Physics 155, 410–438 (1999).\n\n\n\nG. Allaire, F. Jouve and A.-M. Toader. Structural optimization using sensitivity analysis and a level-set method. Journal of computational physics 194, 363–393 (2004).\n\n\n\n","title":"Bibliography"},{"category":"section","location":"reference/#Index","page":"Reference","text":"Pages = [\"reference.md\"]","title":"Index"},{"category":"type","location":"reference/#LevelSetMethods.AbstractMeshField","page":"Reference","text":"abstract type AbstractMeshField{N,T,V}\n\nA mutable, node-centered discrete field on a CartesianGrid: it attaches a value to every node of the grid and can be both read and written by Cartesian multi-index.\n\nThe type parameters expose the field's basic shape: N is the spatial dimension, T the grid's coordinate type, and V the type of the stored values (e.g. Float64 for a level set, SVector{N,Float64} for a velocity field).\n\nInterface\n\nA concrete subtype must implement:\n\nmesh(ϕ): the underlying CartesianGrid; every geometric quantity (ndims, axes, meshsize, …) derives from it.\nϕ[I] (Base.getindex): the value at node I. This must also return a value for indices just outside the grid, so that finite-difference stencils work near the boundary.\nϕ[I] = v (Base.setindex!): write v at node I.\n\nThe following optional methods can be implemented for efficiency:\n\nactive_nodeindices(ϕ): node indices the field is actually defined on; defaults to nodeindices(ϕ) (every grid node).\nactive_cellindices(ϕ): likewise for cells; defaults to cellindices(ϕ).\ncopy(ϕ): clone the field; defaults to deepcopy. The time integrators allocate stage buffers with it, so overriding it to share the immutable mesh/bcs is cheaper.\ncopy!(dest, src): overwrite dest's values (and active set) with src's; a generic fallback copies through values. The time integrators recycle stage buffers with it.\n\nSee MeshField (dense) and NarrowBandMeshField (sparse band) for examples.\n\n\n\n\n\n","title":"LevelSetMethods.AbstractMeshField"},{"category":"type","location":"reference/#LevelSetMethods.AdvectionTerm","page":"Reference","text":"AdvectionTerm(𝐮[, scheme = WENO5(), update_func = nothing])\n\nAdvection term representing  𝐮 ⋅ ∇ϕ. Available schemes are Upwind and WENO5.\n\nIf passed, update_func will be called as update_func(𝐮, ϕ, t) before computing the term at each stage of the time evolution. This can be used to update the velocity field 𝐮 depending not only on t, but also on the current level set ϕ.\n\n\n\n\n\n","title":"LevelSetMethods.AdvectionTerm"},{"category":"type","location":"reference/#LevelSetMethods.BernsteinPolynomial","page":"Reference","text":"struct BernsteinPolynomial{N, T}\n\nA multidimensional Bernstein polynomial with coefficients of type T in N dimensions, defined on a hyperrectangle [low_corner[i], high_corner[i]].\n\n\n\n\n\n","title":"LevelSetMethods.BernsteinPolynomial"},{"category":"method","location":"reference/#LevelSetMethods.BernsteinPolynomial-Tuple{AbstractArray, Any, Any}","page":"Reference","text":"BernsteinPolynomial(c::AbstractArray, lc, hc)\n\nCreate a multidimensional Bernstein polynomial with coefficients c defined on the hyperrectangle [lc[1], hc[1]] × … × [lc[N], hc[N]].\n\nCalling p(x) evaluates the polynomial at the point x = (x[1], …, x[N]) by the formula\n\np(x_1dotsx_D)=sum_i_j=0^d_jc_i_1dots i_Dprod_j=1^Dbinomd_ji_j(x_j-l_j)^i_j(r_j-x_j)^d_j-i_j\n\nwhere l_j = lcj and r_j = hcj are the lower and upper bounds of the hyperrectangle, respectively, and d_j = size(c)j - 1 is the degree of the polynomial in dimension j.\n\nBernsteinPolynomials can be differentiated using ForwardDiff; see gradient and hessian.\n\n\n\n\n\n","title":"LevelSetMethods.BernsteinPolynomial"},{"category":"type","location":"reference/#LevelSetMethods.BoundaryCondition","page":"Reference","text":"abstract type BoundaryCondition\n\nAbstract supertype for boundary conditions. A boundary condition determines the value of a field at ghost indices that lie outside the mesh, closing finite-difference stencils near the domain boundary.\n\nA concrete subtype bc must implement:\n\nbc_stencil(bc, I, ax, dim): return an iterable of (weight, index) pairs whose weighted sum Σ wⱼ · ϕ[Iⱼ] gives the ghost value at the out-of-bounds index I along dimension dim (with ax the in-bounds range). dim is part of the signature because an index can be out of bounds in several dimensions at once (e.g. a corner ghost), and each returned index is resolved along the remaining dimensions by the caller.\n\nbc_stencil is a pure function of indices: it never touches the field, so the field-reading and accumulation live entirely in the indexing code (see _getindexbc).\n\n\n\n\n\n","title":"LevelSetMethods.BoundaryCondition"},{"category":"type","location":"reference/#LevelSetMethods.CartesianCell","page":"Reference","text":"struct CartesianCell{N, T}\n\nA cell of a CartesianGrid: the axis-aligned hypercube bounded by nodes at lc (lower corner) and hc (upper corner). Obtain via getcell(grid, I) where I is a cell index.\n\n\n\n\n\n","title":"LevelSetMethods.CartesianCell"},{"category":"method","location":"reference/#LevelSetMethods.CartesianGrid-Tuple{Any, Any, Any}","page":"Reference","text":"CartesianGrid(lc, hc, n)\n\nCreate a uniform cartesian grid with lower corner lc, upper corner hc and n nodes in each direction.\n\nNode coordinates are accessed via getnode; cell geometry via getcell. Use nodeindices to iterate over all node indices and cellindices to iterate over all cell indices.\n\nExamples\n\nusing LevelSetMethods\na = (0, 0)\nb = (1, 1)\nn = (10, 4)\ngrid = CartesianGrid(a, b, n)\n\n# output\n\nCartesianGrid in ℝ²\n  ├─ domain:  [0.0, 1.0] × [0.0, 1.0]\n  ├─ nodes:   10 × 4\n  └─ spacing: h = (0.1111, 0.3333)\n\n\n\n\n\n","title":"LevelSetMethods.CartesianGrid"},{"category":"method","location":"reference/#LevelSetMethods.CartesianGrid-Tuple{Any, Any}","page":"Reference","text":"CartesianGrid(lc, hc; meshsize)\n\nCreate a uniform cartesian grid spanning [lc, hc] with node spacing approximately equal to meshsize in each dimension. meshsize may be a scalar (applied to every dimension) or a collection with one entry per dimension.\n\nThe domain [lc, hc] is honored exactly; the number of cells in each dimension is rounded up, so the realized spacing (see meshsize) is never coarser than meshsize but may be finer.\n\nExamples\n\nusing LevelSetMethods\nCartesianGrid((0, 0), (1, 1); meshsize = 0.3)\n\n# output\n\nCartesianGrid in ℝ²\n  ├─ domain:  [0.0, 1.0] × [0.0, 1.0]\n  ├─ nodes:   5 × 5\n  └─ spacing: h = (0.25, 0.25)\n\n\n\n\n\n","title":"LevelSetMethods.CartesianGrid"},{"category":"type","location":"reference/#LevelSetMethods.CurvatureTerm","page":"Reference","text":"struct CurvatureTerm{V} <: LevelSetTerm\n\nLevel-set curvature term representing bκ|∇ϕ|, where κ = ∇ ⋅ (∇ϕ/|∇ϕ|) is the curvature.\n\n\n\n\n\n","title":"LevelSetMethods.CurvatureTerm"},{"category":"type","location":"reference/#LevelSetMethods.EikonalReinitializationTerm","page":"Reference","text":"struct EikonalReinitializationTerm <: LevelSetTerm\n\nA LevelSetTerm representing sign(ϕ)(|∇ϕ| - 1), which drives the level set toward a signed distance function by solving the Eikonal equation |∇ϕ| = 1 via pseudo-time marching.\n\nnote: Comparison with `reinitialize!`\nreinitialize! (the Newton closest-point method) is generally preferred: it is applied between time steps (not inside the PDE), preserves the interface to high order, and converges in a single pass. EikonalReinitializationTerm is a simpler alternative that requires no interpolation or KDTree, but needs many time steps to propagate corrections from the interface and can cause mass loss.\n\nThere are two constructors:\n\nEikonalReinitializationTerm(ϕ₀): freezes the sign from the initial level set ϕ₀ (equation 7.5 of Osher & Fedkiw). Recommended when the interface may drift. ϕ₀ may be a full-grid or narrow-band field; the frozen sign lives on the same active set.\nEikonalReinitializationTerm(): recomputes the sign from the current ϕ at each step (equation 7.6 of Osher & Fedkiw).\n\n\n\n\n\n","title":"LevelSetMethods.EikonalReinitializationTerm"},{"category":"type","location":"reference/#LevelSetMethods.ExtrapolationBC","page":"Reference","text":"struct ExtrapolationBC{P} <: BoundaryCondition\n\nDegree-P one-sided polynomial extrapolation boundary condition. Uses the P+1 nearest interior cells to construct a degree-P polynomial that is extrapolated into the ghost region.\n\nExtrapolationBC{0} (aliased as NeumannBC) gives constant extension (∂ϕ/∂n = 0 at the boundary face). ExtrapolationBC{1} (aliased as LinearExtrapolationBC) gives linear extrapolation (∂²ϕ/∂n² = 0).\n\n\n\n\n\n","title":"LevelSetMethods.ExtrapolationBC"},{"category":"type","location":"reference/#LevelSetMethods.ForwardEuler","page":"Reference","text":"struct ForwardEuler\n\nFirst-order explicit Forward Euler time integration scheme.\n\nusing LevelSetMethods\nForwardEuler()\n\n# output\n\nForwardEuler (1st order explicit)\n  └─ cfl: 0.5\n\n\n\n\n\n","title":"LevelSetMethods.ForwardEuler"},{"category":"type","location":"reference/#LevelSetMethods.InterpolatedField","page":"Reference","text":"mutable struct InterpolatedField{F, T, B}\n\nA continuous field obtained by equipping a discrete AbstractMeshField with piecewise polynomial interpolation. Evaluating cf(x) returns the value of the local BernsteinPolynomial on the cell containing x; gradient and hessian differentiate the same local patch.\n\nThe Bernstein basis is chosen for its convex-hull property: the interpolant on a cell is bounded by the extrema of its coefficients, which proven_empty exploits to discard cells with no interface or interior.\n\nConstruct via InterpolatedField(ϕ, order).\n\nnote: Thread safety\nEvaluating concurrently from multiple tasks is safe (each task gets its own scratch; the shared mat is only read). Mutating the field (setindex!, copy!) while other tasks evaluate it is not safe; mutations bump the gen counter, which invalidates every task's cached cell coefficients.\n\n\n\n\n\n","title":"LevelSetMethods.InterpolatedField"},{"category":"type","location":"reference/#LevelSetMethods.InterpolationScratch","page":"Reference","text":"mutable struct InterpolationScratch{N, T}\n\nPer-task working memory and per-cell memo for evaluating an InterpolatedField. It holds the scratch arrays written on every cell fill (coeffs, vals, temp1, temp2) together with the memo key (Ic, gen) identifying which cell the current coeffs are for. It carries no interpolation matrix (that is shared by the field) and no reference to the underlying field.\n\nConstruct via InterpolationScratch(N, order, T).\n\nwarning: Task confinement\nAll fields are mutable state and must be used by one task at a time; InterpolatedField guarantees this by keeping one InterpolationScratch per task (via OncePerTask).\n\n\n\n\n\n","title":"LevelSetMethods.InterpolationScratch"},{"category":"method","location":"reference/#LevelSetMethods.InterpolationScratch-Union{Tuple{T}, Tuple{Int64, Int64, Type{T}}} where T","page":"Reference","text":"InterpolationScratch(N::Int, order::Int, T::Type)\n\nAllocate the per-task scratch for N-dimensional fields with element type T and polynomial interpolation of degree order.\n\n\n\n\n\n","title":"LevelSetMethods.InterpolationScratch"},{"category":"method","location":"reference/#LevelSetMethods.LevelSetEquation-Tuple{}","page":"Reference","text":"LevelSetEquation(; terms, ic, bc, t = 0, integrator = RK2())\n\nCreate a level-set equation of the form ϕₜ + sum(terms) = 0, where each t ∈ terms is a LevelSetTerm and ic is the initial condition — either a MeshField for a full-grid discretization or a NarrowBandMeshField for a narrow-band discretization.\n\nCalling integrate!(eq, tf) will evolve the equation up to time tf, modifying current_state(eq) and current_time(eq) in place. The initial condition ic is copied into the equation, so the field you pass in is never modified; reach the evolving state through current_state.\n\nBoundary conditions come from the bc keyword, or from ic if it already carries them (passing both warns and bc wins); it is an error if neither supplies them. If a single BoundaryCondition is provided, it will be applied uniformly to all boundaries of the domain. To apply different boundary conditions to each boundary, pass a tuple of the form (bc_x, bc_y, ...) with as many elements as dimensions in the domain. If bc_x is a BoundaryCondition, it will be applied to both boundaries in the x direction. If bc_x is a tuple of two BoundaryConditions, the first will be applied to the left boundary and the second to the right boundary. The same logic applies to the other dimensions.\n\nThe optional parameter t specifies the initial time of the simulation, and integrator is the TimeIntegrator used to evolve the level-set equation.\n\nusing LevelSetMethods, StaticArrays\ngrid = CartesianGrid((-1, -1), (1, 1), (50, 50))    # define the grid\nϕ = MeshField(x -> x[1]^2 + x[2]^2 - 0.5^2, grid)    # initial shape\n𝐮 = MeshField(x -> SVector(1, 0), grid)             # advection velocity\nterms = (AdvectionTerm(𝐮),)                          # advection term\nbc = NeumannBC()                                     # zero-gradient boundary conditions\neq = LevelSetEquation(; terms, ic = ϕ, bc)          # level-set equation\n\n# output\n\nLevelSetEquation\n  ├─ equation: ϕₜ + 𝐮 ⋅ ∇ ϕ = 0\n  ├─ time:     0.0\n  ├─ integrator: RK2 (2nd order TVD Runge-Kutta, Heun's method)\n  │  └─ cfl: 0.5\n  ├─ state: MeshField on CartesianGrid in ℝ²\n  │  ├─ domain:  [-1.0, 1.0] × [-1.0, 1.0]\n  │  ├─ nodes:   50 × 50\n  │  ├─ spacing: h = (0.04082, 0.04082)\n  │  ├─ bc:     Neumann (all)\n  │  ├─ valtype: Float64\n  │  └─ values:  min = -0.2492,  max = 1.75\n  ╰─\n\n\n\n\n\n","title":"LevelSetMethods.LevelSetEquation"},{"category":"type","location":"reference/#LevelSetMethods.LevelSetTerm","page":"Reference","text":"abstract type LevelSetTerm\n\nA typical term in a level-set evolution equation.\n\n\n\n\n\n","title":"LevelSetMethods.LevelSetTerm"},{"category":"type","location":"reference/#LevelSetMethods.LinearExtrapolationBC","page":"Reference","text":"LinearExtrapolationBC = ExtrapolationBC{1}\n\nAlias for ExtrapolationBC{1}: linear extrapolation into ghost cells. Corresponds to ∂²ϕ/∂n² = 0 at the boundary face.\n\n\n\n\n\n","title":"LevelSetMethods.LinearExtrapolationBC"},{"category":"type","location":"reference/#LevelSetMethods.MeshField","page":"Reference","text":"struct MeshField{N,T,V,B} <: AbstractMeshField{N,T,V}\n\nA node-centered field defined on the entire mesh, described by its discrete values at each node.\n\nvals: dense Array{V,N} of values at each node.\nmesh: the underlying CartesianGrid{N,T}.\nbcs: boundary conditions, used for indexing outside the mesh bounds.\n\nBase.getindex(ϕ, I) returns the value of the field at node index I. If I lies outside the mesh bounds, bcs are applied to determine the value.\n\nUse nodeindices and cellindices to iterate over active node and cell indices respectively.\n\n\n\n\n\n","title":"LevelSetMethods.MeshField"},{"category":"method","location":"reference/#LevelSetMethods.MeshField-Tuple{Any, CartesianGrid}","page":"Reference","text":"MeshField(vals, grid::CartesianGrid; bc=nothing)\n\nConstruct a node-centered MeshField with explicit values on a grid.\n\nbc: boundary conditions (normalized via _normalize_bc).\n\n\n\n\n\n","title":"LevelSetMethods.MeshField"},{"category":"method","location":"reference/#LevelSetMethods.MeshField-Tuple{Function, CartesianGrid}","page":"Reference","text":"MeshField(f::Function, grid; kwargs...)\n\nCreate a node-centered MeshField by evaluating f at each node of grid.  All keyword arguments are passed to the vals-based constructor.\n\nExamples\n\nusing LevelSetMethods, StaticArrays\ngrid = CartesianGrid((-1, -1), (1, 1), (5, 5))\n# scalar field without boundary conditions\nMeshField(x -> x[1]^2 + x[2]^2 - 0.5^2, grid)\n\n# output\n\nMeshField on CartesianGrid in ℝ²\n  ├─ domain:  [-1.0, 1.0] × [-1.0, 1.0]\n  ├─ nodes:   5 × 5\n  ├─ spacing: h = (0.5, 0.5)\n  ├─ valtype: Float64\n  └─ values:  min = -0.25,  max = 1.75\n\n\n\n\n\n","title":"LevelSetMethods.MeshField"},{"category":"type","location":"reference/#LevelSetMethods.NarrowBandMeshField","page":"Reference","text":"struct NarrowBandMeshField{N,T,V,B} <: AbstractMeshField{N,T,V}\n\nA node-centered field defined on a narrow band: a sparse set of active nodes, with values stored only there. The fields are:\n\nvals: sparse Dict{CartesianIndex{N},V} mapping active node indices to values.\nmesh: the underlying CartesianGrid{N,T}.\nbcs: boundary conditions, used for indexing outside the mesh bounds.\nnlayers: halo depth of the band — the number of node layers grown around the cut cells by update_band!, and the depth it rebuilds to during evolution.\n\nUse active_nodeindices and active_cellindices to iterate over active node and cell indices respectively.\n\nIndexing is defined on the band and the surrounding stencil halo; an in-grid node farther out throws. At present, periodic boundary conditions are not supported.\n\n\n\n\n\n","title":"LevelSetMethods.NarrowBandMeshField"},{"category":"method","location":"reference/#LevelSetMethods.NarrowBandMeshField-Tuple{Any, CartesianGrid}","page":"Reference","text":"NarrowBandMeshField(vals, grid; bc=nothing, nlayers=3)\n\nConstruct a NarrowBandMeshField from a pre-built vals dict whose keys are the active nodes. Useful for an auxiliary field on an adopted band (e.g. a velocity), where the active set is filled from another field rather than derived from vals.\n\n\n\n\n\n","title":"LevelSetMethods.NarrowBandMeshField"},{"category":"method","location":"reference/#LevelSetMethods.NarrowBandMeshField-Tuple{Function, CartesianGrid}","page":"Reference","text":"NarrowBandMeshField(f::Function, grid::CartesianGrid; bc=nothing, nlayers=3)\n\nConstruct a narrow-band field by evaluating f at each node of grid, then restricting to a band of nlayers around the interface. Equivalent to NarrowBandMeshField(MeshField(f, grid; bc); nlayers).\n\n\n\n\n\n","title":"LevelSetMethods.NarrowBandMeshField"},{"category":"method","location":"reference/#LevelSetMethods.NarrowBandMeshField-Tuple{MeshField}","page":"Reference","text":"NarrowBandMeshField(ϕ::MeshField; nlayers = 3)\n\nConstruct a topological narrow-band field from a full-grid MeshField. The narrow-band field is defined on the band and a halo of nlayers nodes around the cut-cells.\n\n\n\n\n\n","title":"LevelSetMethods.NarrowBandMeshField"},{"category":"type","location":"reference/#LevelSetMethods.NeumannBC","page":"Reference","text":"const NeumannBC = ExtrapolationBC{0}\n\nHomogeneous Neumann boundary condition (∂ϕ/∂n = 0 at the boundary face). Alias for ExtrapolationBC{0}: ghost cells take the value of the nearest boundary node (constant extension).\n\n\n\n\n\n","title":"LevelSetMethods.NeumannBC"},{"category":"type","location":"reference/#LevelSetMethods.NewtonSDF","page":"Reference","text":"struct NewtonSDF{Φ,Tr,P,T}\n\nA signed distance function to the zero level set of an underlying level set function, computed using a Newton-based closest point method.\n\nEvaluating sdf(x) returns the signed distance from point x to the interface. An optional second argument sdf(x, s) can be used to supply the sign directly (e.g. sign(ϕ(x))) when it is already known, avoiding an extra interpolant evaluation.\n\nnote: Thread safety\nEvaluating a NewtonSDF concurrently from multiple tasks is safe: the underlying InterpolatedField keeps one scratch buffer per task, and the KDTree and sample points are read-only during evaluation.\n\n\n\n\n\n","title":"LevelSetMethods.NewtonSDF"},{"category":"method","location":"reference/#LevelSetMethods.NewtonSDF-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"NewtonSDF(ϕ::AbstractMeshField; order=3, upsample=2, maxiters=10, xtol=nothing, ftol=nothing)\n\nConstruct a NewtonSDF from an [AbstractMeshField]. The interface is sampled by projecting uniformly-spaced points in each cell onto the zero level set, building a KDTree for fast nearest-neighbour queries. Given a query point, the closest interface point is found by seeding a Newton-Lagrange solve from the nearest sample.\n\nKeyword arguments\n\norder: polynomial interpolation order\nupsample: sampling density per cell side\nmaxiters: maximum Newton iterations\nxtol: tolerance on the KKT residual; defaults to sqrt(eps(T)) with T = valtype(ϕ)\nftol: tolerance on the function value; defaults to sqrt(eps(T)) with T = valtype(ϕ)\n\n\n\n\n\n","title":"LevelSetMethods.NewtonSDF"},{"category":"type","location":"reference/#LevelSetMethods.NormalMotionTerm","page":"Reference","text":"struct NormalMotionTerm{V,F} <: LevelSetTerm\n\nLevel-set advection term representing  v |∇ϕ|. This LevelSetTerm should be used for internally generated velocity fields; for externally generated velocities you may use AdvectionTerm instead.\n\nIf passed, update_func will be called as update_func(v, ϕ, t) before computing the term at each stage of the time evolution.\n\n\n\n\n\n","title":"LevelSetMethods.NormalMotionTerm"},{"category":"type","location":"reference/#LevelSetMethods.PeriodicBC","page":"Reference","text":"struct PeriodicBC <: BoundaryCondition\n\nSingleton type representing periodic boundary conditions. Ghost cells are filled by wrapping around to the opposite side of the domain.\n\n\n\n\n\n","title":"LevelSetMethods.PeriodicBC"},{"category":"type","location":"reference/#LevelSetMethods.RK2","page":"Reference","text":"struct RK2\n\nSecond order total variation dimishing Runge-Kutta scheme, also known as Heun's predictor-corrector method.\n\nusing LevelSetMethods\nRK2()\n\n# output\n\nRK2 (2nd order TVD Runge-Kutta, Heun's method)\n  └─ cfl: 0.5\n\n\n\n\n\n","title":"LevelSetMethods.RK2"},{"category":"type","location":"reference/#LevelSetMethods.RK3","page":"Reference","text":"struct RK3\n\nThird order total variation dimishing Runge-Kutta scheme.\n\nusing LevelSetMethods\nRK3()\n\n# output\n\nRK3 (3rd order TVD Runge-Kutta)\n  └─ cfl: 0.5\n\n\n\n\n\n","title":"LevelSetMethods.RK3"},{"category":"type","location":"reference/#LevelSetMethods.SemiImplicitI2OE","page":"Reference","text":"struct SemiImplicitI2OE\n\nSemi-implicit finite-volume scheme of the I2OE family ([3]) for advection problems.\n\nusing LevelSetMethods\nSemiImplicitI2OE()\n\n# output\n\nSemiImplicitI2OE (semi-implicit advection, Mikula et al.)\n  └─ cfl: 2.0\n\n\n\n\n\n","title":"LevelSetMethods.SemiImplicitI2OE"},{"category":"type","location":"reference/#LevelSetMethods.SymmetryBC","page":"Reference","text":"struct SymmetryBC <: BoundaryCondition\n\nSymmetry-plane (reflective) boundary condition: the field is mirrored across the boundary node, ϕ[b - k] = ϕ[b + k]. Like NeumannBC it enforces ∂ϕ/∂n = 0, but by reflection rather than flat extension, so it also preserves curvature symmetrically and makes the interface meet the boundary perpendicularly. This is the correct condition on a symmetry axis, e.g. for axisymmetric simulations.\n\n\n\n\n\n","title":"LevelSetMethods.SymmetryBC"},{"category":"type","location":"reference/#LevelSetMethods.TimeIntegrator","page":"Reference","text":"abstract type TimeIntegrator end\n\nAbstract type for time integrators. See subtypes(TimeIntegrator) for a list of available time integrators. Every concrete integrator stores a cfl safety factor.\n\n\n\n\n\n","title":"LevelSetMethods.TimeIntegrator"},{"category":"type","location":"reference/#LevelSetMethods.Upwind","page":"Reference","text":"Upwind <: SpatialScheme\n\nFirst-order upwind discretization of the spatial derivatives in a level-set term. Cheap and robust, but introduces significant numerical diffusion; prefer WENO5 unless cost is the overriding concern.\n\n\n\n\n\n","title":"LevelSetMethods.Upwind"},{"category":"type","location":"reference/#LevelSetMethods.WENO5","page":"Reference","text":"WENO5 <: SpatialScheme\n\nFifth-order WENO (Weighted Essentially Non-Oscillatory) discretization of the spatial derivatives in a level-set term. More expensive than Upwind, but much more accurate in smooth regions while suppressing oscillations near steep gradients.\n\n\n\n\n\n","title":"LevelSetMethods.WENO5"},{"category":"method","location":"reference/#Base.copy!-Tuple{LevelSetMethods.AbstractMeshField, LevelSetMethods.AbstractMeshField}","page":"Reference","text":"Base.copy!(dest::AbstractMeshField, src::AbstractMeshField)\n\nMake dest hold the same values as src, leaving its mesh and boundary conditions untouched. For a sparse NarrowBandMeshField this also replaces dest's active set with src's (the Dict copy! syncs keys), so the two agree on which nodes are stored — the time integrator relies on this to keep a reused stage buffer's band in sync with the evolving field.\n\n\n\n\n\n","title":"Base.copy!"},{"category":"method","location":"reference/#Base.copy-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"Base.copy(ϕ::AbstractMeshField)\n\nClone ϕ. Generic fallback (deepcopy); concrete fields override it to share their immutable mesh and boundary conditions and only copy the values.\n\n\n\n\n\n","title":"Base.copy"},{"category":"method","location":"reference/#Base.copy-Tuple{MeshField}","page":"Reference","text":"copy(ϕ::MeshField)\n\nReturn a MeshField with an independent copy of the values of ϕ, sharing its (immutable) mesh and boundary conditions.\n\n\n\n\n\n","title":"Base.copy"},{"category":"method","location":"reference/#Base.copy-Tuple{NarrowBandMeshField}","page":"Reference","text":"copy(nb::NarrowBandMeshField)\n\nReturn a NarrowBandMeshField with an independent copy of the band values of nb, sharing its (immutable) mesh and boundary conditions.\n\n\n\n\n\n","title":"Base.copy"},{"category":"method","location":"reference/#Base.intersect!-Tuple{MeshField, MeshField}","page":"Reference","text":"intersect!(ϕ₁::MeshField, ϕ₂::MeshField)\n\nIn-place intersection of two level sets: ϕ₁ = max(ϕ₁, ϕ₂).\n\n\n\n\n\n","title":"Base.intersect!"},{"category":"method","location":"reference/#Base.intersect-Tuple{MeshField, MeshField}","page":"Reference","text":"intersect(ϕ₁::MeshField, ϕ₂::MeshField)\n\nIntersection of two level sets: max(ϕ₁, ϕ₂).\n\n\n\n\n\n","title":"Base.intersect"},{"category":"method","location":"reference/#Base.map-Tuple{Any, MeshField}","page":"Reference","text":"map(f, ϕ::MeshField)\n\nReturn a MeshField on the same mesh and boundary conditions as ϕ, with each value replaced by f applied to it. The element type may change with f.\n\n\n\n\n\n","title":"Base.map"},{"category":"method","location":"reference/#Base.map-Tuple{Any, NarrowBandMeshField}","page":"Reference","text":"map(f, nb::NarrowBandMeshField)\n\nReturn a NarrowBandMeshField with the same mesh, boundary conditions and band as nb, with each stored value replaced by f applied to it. The element type may change with f.\n\n\n\n\n\n","title":"Base.map"},{"category":"method","location":"reference/#Base.setdiff!-Tuple{MeshField, MeshField}","page":"Reference","text":"setdiff!(ϕ₁::MeshField, ϕ₂::MeshField)\n\nIn-place set difference: ϕ₁ = max(ϕ₁, -ϕ₂).\n\n\n\n\n\n","title":"Base.setdiff!"},{"category":"method","location":"reference/#Base.setdiff-Tuple{MeshField, MeshField}","page":"Reference","text":"setdiff(ϕ₁::MeshField, ϕ₂::MeshField)\n\nSet difference of two level sets: max(ϕ₁, -ϕ₂).\n\n\n\n\n\n","title":"Base.setdiff"},{"category":"method","location":"reference/#Base.union!-Tuple{MeshField, MeshField}","page":"Reference","text":"union!(ϕ₁::MeshField, ϕ₂::MeshField)\n\nIn-place union of two level sets: ϕ₁ = min(ϕ₁, ϕ₂).\n\n\n\n\n\n","title":"Base.union!"},{"category":"method","location":"reference/#Base.union-Tuple{MeshField, MeshField}","page":"Reference","text":"union(ϕ₁::MeshField, ϕ₂::MeshField)\n\nUnion of two level sets: min(ϕ₁, ϕ₂).\n\n\n\n\n\n","title":"Base.union"},{"category":"method","location":"reference/#LevelSetMethods.D2-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Tuple{Int64, Int64}}","page":"Reference","text":"D2(ϕ,I,dims)\n\nFinite difference scheme for second order derivative at grid point I along the dimensions dims.\n\nIf dims[1] == dims[2], it is more efficient to call D2⁰(ϕ,I,dims[1]).\n\n\n\n\n\n","title":"LevelSetMethods.D2"},{"category":"method","location":"reference/#LevelSetMethods.D2⁰-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Int64}","page":"Reference","text":"D2⁰(ϕ,I,dim)\n\nCentered finite difference scheme for second order derivative at grid point I along dimension dim. E.g. if dim=1, this approximates ∂ₓₓ.\n\n\n\n\n\n","title":"LevelSetMethods.D2⁰"},{"category":"method","location":"reference/#LevelSetMethods.D2⁺⁺-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Int64}","page":"Reference","text":"D2⁺⁺(ϕ,I,dim)\n\nForward finite difference scheme for second order derivative at grid point I along dimension dim. E.g. if dim=1, this approximates ∂ₓₓ.\n\n\n\n\n\n","title":"LevelSetMethods.D2⁺⁺"},{"category":"method","location":"reference/#LevelSetMethods.D2⁻⁻-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Int64}","page":"Reference","text":"D2⁻⁻(ϕ,I,dim)\n\nBackward finite difference scheme for second order derivative at grid point I along dimension dim. E.g. if dim=1, this approximates ∂ₓₓ.\n\n\n\n\n\n","title":"LevelSetMethods.D2⁻⁻"},{"category":"method","location":"reference/#LevelSetMethods.D⁰-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Int64}","page":"Reference","text":"D⁰(ϕ,I,dim)\n\nCentered finite difference scheme for first order derivative at grid point I along dimension dim.\n\n\n\n\n\n","title":"LevelSetMethods.D⁰"},{"category":"method","location":"reference/#LevelSetMethods.D⁺-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Int64}","page":"Reference","text":"D⁺(ϕ,I,dim)\n\nForward finite difference scheme for first order derivative at grid point I along dimension dim.\n\n\n\n\n\n","title":"LevelSetMethods.D⁺"},{"category":"method","location":"reference/#LevelSetMethods.D⁻-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Int64}","page":"Reference","text":"D⁻(ϕ,I,dim)\n\nBackward finite difference scheme for first order derivative at grid point I along dimension dim.\n\n\n\n\n\n","title":"LevelSetMethods.D⁻"},{"category":"method","location":"reference/#LevelSetMethods._add_boundary_conditions-Tuple{MeshField, Any}","page":"Reference","text":"_add_boundary_conditions(ϕ::MeshField, bc)\n\nReturn a new MeshField with bc as boundary conditions.  All of the underlying data is aliased (shared) with the original MeshField.\n\n\n\n\n\n","title":"LevelSetMethods._add_boundary_conditions"},{"category":"method","location":"reference/#LevelSetMethods._add_boundary_conditions-Tuple{NarrowBandMeshField, Any}","page":"Reference","text":"_add_boundary_conditions(nb::NarrowBandMeshField, bc)\n\nReturn a new NarrowBandMeshField with bc as boundary conditions. All underlying data is aliased with the original.\n\n\n\n\n\n","title":"LevelSetMethods._add_boundary_conditions"},{"category":"method","location":"reference/#LevelSetMethods._bc_str-Tuple{Tuple}","page":"Reference","text":"_bc_str(bcs)\n\nFormat a boundary-conditions tuple (as returned by boundary_conditions) into a compact human-readable string. Each element of bcs is a (left, right) pair for one spatial dimension.\n\n\n\n\n\n","title":"LevelSetMethods._bc_str"},{"category":"method","location":"reference/#LevelSetMethods._closest_point-Union{Tuple{T}, Tuple{N}, Tuple{LevelSetMethods.BernsteinPolynomial, SVector{N, T}, SVector{N, T}, Vararg{Any, 4}}} where {N, T}","page":"Reference","text":"_closest_point(p::BernsteinPolynomial, xq, x0, maxiters, xtol, ftol, safeguard_dist) -> (x_closest, converged)\n\nFind the point on the zero level-set of the single-cell patch p closest to xq, starting from x0. Uses a Newton-Lagrange solver on the KKT conditions of min ||x - xq||² s.t. p(x) = 0. The patch is fixed: unlike _project_to_interface, the iterate is not re-dispatched to neighbouring cells.\n\n\n\n\n\n","title":"LevelSetMethods._closest_point"},{"category":"method","location":"reference/#LevelSetMethods._closest_point_from_seed-Tuple{LevelSetMethods.NewtonSDF, SVector, SVector}","page":"Reference","text":"_closest_point_from_seed(sdf, x, seed)\n\nRun the Newton-Lagrange closest-point solver on the Bernstein patch of the cell containing seed (a point already on or near the interface). Returns (closest_point, converged, ∇φ(closest_point)), where the gradient gives the (unnormalized, outward) interface normal at the closest point.\n\n\n\n\n\n","title":"LevelSetMethods._closest_point_from_seed"},{"category":"method","location":"reference/#LevelSetMethods._closest_point_on_interface-Tuple{LevelSetMethods.NewtonSDF, Any}","page":"Reference","text":"_closest_point_on_interface(sdf, x)\n\nFind the point on the interface closest to x by seeding a local Newton-Lagrange solve from the nearest interface sample. Returns (closest_point, converged, ∇φ(closest_point)).\n\n\n\n\n\n","title":"LevelSetMethods._closest_point_on_interface"},{"category":"method","location":"reference/#LevelSetMethods._domain_str-Union{Tuple{CartesianGrid{N}}, Tuple{N}} where N","page":"Reference","text":"_domain_str(g::CartesianGrid) -> String\n\nFormat the domain as \"[lo, hi] × [lo, hi] × …\".\n\n\n\n\n\n","title":"LevelSetMethods._domain_str"},{"category":"method","location":"reference/#LevelSetMethods._embed_show-Tuple{Any, Any, Any}","page":"Reference","text":"_embed_show(io, label, obj; indent=\"  \")\n\nPrint the text/plain representation of obj indented under label as a tree branch. The first line becomes ├─ label: <header>, and any remaining lines are indented with │.\n\n\n\n\n\n","title":"LevelSetMethods._embed_show"},{"category":"method","location":"reference/#LevelSetMethods._extrapolate_to_ghost-Union{Tuple{N}, Tuple{NarrowBandMeshField{N}, Any}} where N","page":"Reference","text":"_extrapolate_to_ghost(nb::NarrowBandMeshField, I) -> value\n\nExtrapolate a value at the out-of-band index I so that finite-difference/WENO stencils reaching just past the band edge get a consistent value. It is the linear (affine) Lagrange extrapolant anchored at the nearest band node.\n\n\n\n\n\n","title":"LevelSetMethods._extrapolate_to_ghost"},{"category":"method","location":"reference/#LevelSetMethods._getindexbc-Union{Tuple{dim}, Tuple{LevelSetMethods.AbstractMeshField, Any, Val{dim}}} where dim","page":"Reference","text":"_getindexbc(ϕ::AbstractMeshField, I, Val(dim)) -> value\n\nRecursive resolutions ϕ at an out-of-grid index I via boundary conditions. The recursion peels one dimension at a time from dim down to 0; each level that is in range is skipped, and each out-of-range level is resolved by accumulating the boundary condition's bc_stencil, recursing on the remaining dimensions so corner ghosts (out of bounds in several dimensions) compose correctly. At level 0 every component is back in range, so the value is read through the field's own in-grid getindex (ϕ[I]).\n\ndim is carried as a Val so that it is a compile-time constant: the per-dimension lookups axes(ϕ)[dim] and boundary_conditions(ϕ)[dim] then resolve to concrete types, keeping the boundary-condition dispatch static even when the conditions differ between dimensions.\n\n\n\n\n\n","title":"LevelSetMethods._getindexbc"},{"category":"method","location":"reference/#LevelSetMethods._i2oe_relation-Union{Tuple{T}, Tuple{LevelSetMethods.BoundaryCondition, Any, Any, Any, Any, Type{T}}} where T","page":"Reference","text":"_i2oe_relation(bc, Ioff, I, ax, dim, T) -> (α, β, idx, γ)\n\nExpress the ghost value of the SemiImplicitI2OE scheme at the out-of-bounds neighbor Ioff of node I (along dimension dim, with in-bounds range ax and element type T) as the affine relation α·u[I] + β·u[idx] + γ. Optional interface method of BoundaryCondition; the fallback rejects conditions the integrator cannot handle.\n\n\n\n\n\n","title":"LevelSetMethods._i2oe_relation"},{"category":"method","location":"reference/#LevelSetMethods._ingrid_value-Union{Tuple{N}, Tuple{NarrowBandMeshField{N}, Any}} where N","page":"Reference","text":"_ingrid_value(nb::NarrowBandMeshField, I) -> value\n\nValue at the in-grid index I. Returns the stored value if I is a band node; otherwise extrapolates it from the nearest band node (see _extrapolate_to_ghost).\n\n\n\n\n\n","title":"LevelSetMethods._ingrid_value"},{"category":"method","location":"reference/#LevelSetMethods._interpolation_matrix-Union{Tuple{T}, Tuple{Int64, Type{T}}} where T","page":"Reference","text":"_interpolation_matrix(order, T)\n\nBuild the 1D interpolation matrix mapping the stencil_order + 1 equispaced nodal values on a super-cell to the order + 1 coefficients of the Bernstein basis on the central cell.\n\nThe matrix depends only on order and the element type T — not on the cell or the spatial dimension — so it is built once per InterpolatedField and shared (read-only) by every evaluating task.\n\n\n\n\n\n","title":"LevelSetMethods._interpolation_matrix"},{"category":"method","location":"reference/#LevelSetMethods._itp_buffer-Tuple{InterpolatedField}","page":"Reference","text":"_itp_buffer(cf::InterpolatedField)\n\nReturn the calling task's private InterpolationScratch for cf, lazily created on first use and cached per task by the OncePerTask buffer. No lock is needed and concurrent tasks never share scratch memory.\n\n\n\n\n\n","title":"LevelSetMethods._itp_buffer"},{"category":"method","location":"reference/#LevelSetMethods._lagrange_extrap_weight-Tuple{Int64, Int64, Int64}","page":"Reference","text":"_lagrange_extrap_weight(j, k, P)\n\nLagrange weight for the j-th interior node (0-indexed) when extrapolating to a ghost point at distance k outside the boundary, using a degree-P polynomial fitted to P+1 nodes.\n\nNodes are at positions 0, 1, …, P (relative to the boundary); the ghost is at position -k.  The weight is\n\nTODO: maybe write this as proper latex math?\n\nwⱼ = ∏_{m=0, m≠j}^{P}  (-k - m) / (j - m)\n\n\n\n\n\n","title":"LevelSetMethods._lagrange_extrap_weight"},{"category":"method","location":"reference/#LevelSetMethods._normalize_bc-Tuple{Any, Any}","page":"Reference","text":"_normalize_bc(bc, dim)\n\nNormalize the bc argument into a dim-tuple of (left, right) pairs, one per spatial dimension, as expected by _add_boundary_conditions.\n\nA single BoundaryCondition is applied to all sides.\nA length-dim collection applies each entry to both sides of the corresponding dimension.\nA length-dim collection of 2-tuples applies each entry as (left, right) for that dimension.\n\n\n\n\n\n","title":"LevelSetMethods._normalize_bc"},{"category":"method","location":"reference/#LevelSetMethods._normalize_bc_pair-Tuple{Any, Any}","page":"Reference","text":"_normalize_bc_pair(bc, dim) -> (left, right)\n\nNormalize the boundary condition(s) for a single dimension dim into a (left, right) pair. A single BoundaryCondition applies to both sides; a length-2 collection is taken as (left, right). Periodicity must hold on both sides of a dimension or neither.\n\n\n\n\n\n","title":"LevelSetMethods._normalize_bc_pair"},{"category":"method","location":"reference/#LevelSetMethods._project_to_interface-Tuple{InterpolatedField, Vararg{Any, 4}}","page":"Reference","text":"_project_to_interface(itp::InterpolatedField, x_start, maxiters, ftol, safeguard_dist)\n\nUse Newton's method to project a starting point onto the zero level set of itp. Returns the converged point or nothing if Newton fails to converge or if the iterate moves more than safeguard_dist from x_start. The full InterpolatedField (not a single-cell patch) is used so the projection follows the iterate across cells.\n\n\n\n\n\n","title":"LevelSetMethods._project_to_interface"},{"category":"method","location":"reference/#LevelSetMethods._sample_cell!-Union{Tuple{T}, Tuple{N}, Tuple{Any, Any, CartesianGrid{N, T}, InterpolatedField, Vararg{Any, 5}}} where {N, T}","page":"Reference","text":"_sample_cell!(pts, lk, grid, itp, I, upsample, maxiters, ftol, safeguard_dist)\n\nProject uniformly-spaced sample points in cell I onto the interface, appending to the shared pts (under lock lk) the converged projections that land back inside I. Cells proven to contain no interface are skipped.\n\n\n\n\n\n","title":"LevelSetMethods._sample_cell!"},{"category":"method","location":"reference/#LevelSetMethods._sample_interface-Union{Tuple{T}, Tuple{N}, Tuple{CartesianGrid{N, T}, InterpolatedField, Vararg{Any, 4}}} where {N, T}","page":"Reference","text":"_sample_interface(grid, itp, cells, upsample, maxiters, ftol)\n\nProject uniformly-spaced sample points in each candidate cell onto the interface. Returns all converged projections; cells proven to be empty are skipped. Cells are processed in parallel, appending to a single shared vector guarded by a lock.\n\n\n\n\n\n","title":"LevelSetMethods._sample_interface"},{"category":"method","location":"reference/#LevelSetMethods._show_fields-Union{Tuple{N}, Tuple{IO, CartesianGrid{N}}} where N","page":"Reference","text":"_show_fields(io, g::CartesianGrid; prefix=\"  \", last=true)\n\nPrint domain, nodes, and spacing of g as indented tree lines. When last=true the spacing line uses └─ (terminal); otherwise ├─ (continuing).\n\n\n\n\n\n","title":"LevelSetMethods._show_fields"},{"category":"method","location":"reference/#LevelSetMethods._superscript-Tuple{Int64}","page":"Reference","text":"_superscript(n::Int) -> String\n\nConvert an integer to its Unicode superscript representation, e.g. 2 → \"²\".\n\n\n\n\n\n","title":"LevelSetMethods._superscript"},{"category":"method","location":"reference/#LevelSetMethods.active_cellindices-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"active_cellindices(ϕ::AbstractMeshField)\n\nReturn the cell indices ϕ is defined on. Defaults to cellindices (every grid cell); sparse fields override it with their active set.\n\n\n\n\n\n","title":"LevelSetMethods.active_cellindices"},{"category":"method","location":"reference/#LevelSetMethods.active_cellindices-Union{Tuple{NarrowBandMeshField{N}}, Tuple{N}} where N","page":"Reference","text":"active_cellindices(nb::NarrowBandMeshField)\n\nReturn the set of active cell indices of nb: cells whose corners are all in-band nodes.\n\n\n\n\n\n","title":"LevelSetMethods.active_cellindices"},{"category":"method","location":"reference/#LevelSetMethods.active_nodeindices-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"active_nodeindices(ϕ::AbstractMeshField)\n\nReturn the node indices ϕ is defined on. Defaults to nodeindices (every grid node); sparse fields such as NarrowBandMeshField override it with their active set.\n\n\n\n\n\n","title":"LevelSetMethods.active_nodeindices"},{"category":"method","location":"reference/#LevelSetMethods.active_nodeindices-Tuple{NarrowBandMeshField}","page":"Reference","text":"active_nodeindices(nb::NarrowBandMeshField)\n\nReturn the set of active (in-band) node indices of nb.\n\n\n\n\n\n","title":"LevelSetMethods.active_nodeindices"},{"category":"function","location":"reference/#LevelSetMethods.bc_stencil","page":"Reference","text":"bc_stencil(bc::BoundaryCondition, I, ax, dim) -> NTuple of (weight, index)\n\nExpress the ghost value at the out-of-bounds index I (along dimension dim, with in-bounds range ax) as a weighted sum Σ wⱼ · ϕ[Iⱼ], returned as a tuple of (weight, index) pairs. Each returned index is in range along dim but may still be out of bounds in other dimensions; the caller (_getindexbc) resolves those by recursion. Primary interface method of BoundaryCondition; pure function of indices, with no field access.\n\n\n\n\n\n","title":"LevelSetMethods.bc_stencil"},{"category":"method","location":"reference/#LevelSetMethods.boundary_conditions-Tuple{LevelSetEquation}","page":"Reference","text":"boundary_conditions(eq::LevelSetEquation)\n\nReturn the boundary conditions of the equation.\n\n\n\n\n\n","title":"LevelSetMethods.boundary_conditions"},{"category":"method","location":"reference/#LevelSetMethods.cell_extrema-Tuple{InterpolatedField, CartesianIndex}","page":"Reference","text":"cell_extrema(cf::InterpolatedField, I::CartesianIndex)\n\nCompute the minimum and maximum values of the local Bernstein interpolant in cell I.\n\n\n\n\n\n","title":"LevelSetMethods.cell_extrema"},{"category":"method","location":"reference/#LevelSetMethods.cellindices-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"cellindices(ϕ::AbstractMeshField)\n\nReturn all cell indices of the underlying mesh. See active_cellindices for the subset a field is actually defined on.\n\n\n\n\n\n","title":"LevelSetMethods.cellindices"},{"category":"method","location":"reference/#LevelSetMethods.cellindices-Union{Tuple{CartesianGrid{N}}, Tuple{N}} where N","page":"Reference","text":"cellindices(g::CartesianGrid)\n\nReturn a CartesianIndices ranging over all cell indices of g. Cell I is the hypercube bounded by nodes I and I + 1 in each dimension. Cells are indexed 1:n[d]-1 in each dimension d.\n\n\n\n\n\n","title":"LevelSetMethods.cellindices"},{"category":"method","location":"reference/#LevelSetMethods.complement!-Tuple{MeshField}","page":"Reference","text":"complement!(ϕ::MeshField)\n\nIn-place complement of a level set: ϕ = -ϕ.\n\n\n\n\n\n","title":"LevelSetMethods.complement!"},{"category":"method","location":"reference/#LevelSetMethods.complement-Tuple{MeshField}","page":"Reference","text":"complement(ϕ::MeshField)\n\nComplement of a level set: -ϕ.\n\n\n\n\n\n","title":"LevelSetMethods.complement"},{"category":"method","location":"reference/#LevelSetMethods.compute_cfl-Tuple{Any, Any, Any}","page":"Reference","text":"compute_cfl(terms, ϕ::MeshField, t)\n\nCompute the maximum stable time-step Δt for the given terms and level set ϕ at time t, based on the Courant-Friedrichs-Lewy (CFL) condition.\n\n\n\n\n\n","title":"LevelSetMethods.compute_cfl"},{"category":"method","location":"reference/#LevelSetMethods.compute_index-Tuple{LevelSetMethods.AbstractMeshField, Any}","page":"Reference","text":"compute_index(ϕ::AbstractMeshField, x)\n\nReturn the multi-index of the cell containing the point x, delegating to compute_index(g::CartesianGrid, x) on the underlying mesh.\n\n\n\n\n\n","title":"LevelSetMethods.compute_index"},{"category":"method","location":"reference/#LevelSetMethods.compute_index-Union{Tuple{N}, Tuple{CartesianGrid{N}, Any}} where N","page":"Reference","text":"compute_index(g::CartesianGrid, x)\n\nReturn the multi-index of the cell of g containing the point x. If x lies outside the grid, the index of the closest cell is returned (clamped to the boundary).\n\n\n\n\n\n","title":"LevelSetMethods.compute_index"},{"category":"method","location":"reference/#LevelSetMethods.current_state-Tuple{LevelSetEquation}","page":"Reference","text":"current_state(eq::LevelSetEquation)\n\nReturn the current state of the level-set equation (a MeshField).\n\n\n\n\n\n","title":"LevelSetMethods.current_state"},{"category":"method","location":"reference/#LevelSetMethods.current_time-Tuple{LevelSetEquation}","page":"Reference","text":"current_time(eq::LevelSetEquation)\n\nReturn the current time of the simulation.\n\n\n\n\n\n","title":"LevelSetMethods.current_time"},{"category":"method","location":"reference/#LevelSetMethods.curvature-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex}","page":"Reference","text":"curvature(ϕ::AbstractMeshField, I::CartesianIndex)\n\nMean curvature κ = ∇ ⋅ (∇ϕ / ‖∇ϕ‖) of the level set of ϕ at grid index I, computed from the gradient and hessian (centered finite differences) via\n\nκ = (Δϕ ‖∇ϕ‖² - ∇ϕᵀ Hϕ ∇ϕ) / ‖∇ϕ‖³ .\n\nReturns zero where ∇ϕ vanishes, since the curvature is undefined there. See the Wikipedia article on the implicit form of the mean curvature.\n\n\n\n\n\n","title":"LevelSetMethods.curvature"},{"category":"method","location":"reference/#LevelSetMethods.export_surface_mesh-Tuple","page":"Reference","text":"export_surface_mesh(eq::LevelSetEquation, filename; kwargs...)\n\nExport a surface mesh of the 3D interface (where ϕ = 0) to filename.\n\nnote: Note\nRequires loading MMG_jll.jl to activate the extension.\n\n\n\n\n\n","title":"LevelSetMethods.export_surface_mesh"},{"category":"method","location":"reference/#LevelSetMethods.export_volume_mesh-Tuple","page":"Reference","text":"export_volume_mesh(eq::LevelSetEquation, filename; kwargs...)\n\nExport a 3D volume mesh of the interior domain (where ϕ < 0) to filename.\n\nnote: Note\nRequires loading MMG_jll.jl to activate the extension.\n\n\n\n\n\n","title":"LevelSetMethods.export_volume_mesh"},{"category":"method","location":"reference/#LevelSetMethods.extend_along_normals!-Union{Tuple{N}, Tuple{T}, Tuple{AbstractArray{T, N}, MeshField}} where {T<:Real, N}","page":"Reference","text":"extend_along_normals!(F, ϕ::MeshField;\n                      nb_iters = 50,\n                      cfl = 0.45,\n                      frozen = nothing,\n                      interface_band = 1.5,\n                      min_norm = 1e-14)\n\nExtend a scalar speed field F away from the interface of ϕ by solving in pseudo-time ∂τF + sign(ϕ) n⋅∇F = 0, with n = ∇ϕ / |∇ϕ|.\n\nThe equation is discretized with first-order upwind derivatives. The update preserves frozen nodes (Dirichlet constraint). If frozen is not provided, a mask is built from the interface band abs(ϕ) <= interface_band * Δ, where Δ = minimum(meshsize(ϕ)).\n\nF can be an AbstractArray (same size as ϕ) or a MeshField on the same mesh.\n\nReference from [Peng et al. 1999]\n\n\n\n\n\n","title":"LevelSetMethods.extend_along_normals!"},{"category":"method","location":"reference/#LevelSetMethods.fill_coefficients!-Union{Tuple{N}, Tuple{LevelSetMethods.AbstractMeshField, Matrix, LevelSetMethods.InterpolationScratch, CartesianIndex{N}}} where N","page":"Reference","text":"fill_coefficients!(ϕ::AbstractMeshField, mat, scratch::InterpolationScratch, base_idxs::CartesianIndex)\n\nFill scratch.coeffs with the Bernstein coefficients for the cell at base_idxs, using the shared interpolation matrix mat and the surrounding stencil of grid values of ϕ.\n\n\n\n\n\n","title":"LevelSetMethods.fill_coefficients!"},{"category":"method","location":"reference/#LevelSetMethods.get_sample_points-Tuple{LevelSetMethods.NewtonSDF}","page":"Reference","text":"get_sample_points(sdf::NewtonSDF)\n\nReturn the interface sample points, lying on the surface, used to build the KDTree of sdf.\n\n\n\n\n\n","title":"LevelSetMethods.get_sample_points"},{"category":"method","location":"reference/#LevelSetMethods.getcell-Tuple{LevelSetMethods.AbstractMeshField, Any}","page":"Reference","text":"getcell(ϕ::AbstractMeshField, I)\n\nReturn the cell at index I, delegating to getcell on the underlying mesh.\n\n\n\n\n\n","title":"LevelSetMethods.getcell"},{"category":"method","location":"reference/#LevelSetMethods.getcell-Union{Tuple{N}, Tuple{CartesianGrid{N}, CartesianIndex{N}}} where N","page":"Reference","text":"getcell(g::CartesianGrid, I::CartesianIndex)\n\nReturn the CartesianCell with lower corner at node I and upper corner at node I+1.  I must be a valid cell index, i.e. I ∈ cellindices(g).\n\n\n\n\n\n","title":"LevelSetMethods.getcell"},{"category":"method","location":"reference/#LevelSetMethods.getnode-Tuple{LevelSetMethods.AbstractMeshField, Any}","page":"Reference","text":"getnode(ϕ::AbstractMeshField, I)\n\nReturn the coordinates of node I, delegating to getnode on the underlying mesh.\n\n\n\n\n\n","title":"LevelSetMethods.getnode"},{"category":"method","location":"reference/#LevelSetMethods.getnode-Union{Tuple{N}, Tuple{CartesianGrid{N}, CartesianIndex{N}}} where N","page":"Reference","text":"getnode(g::CartesianGrid, I::CartesianIndex)\ngetnode(g::CartesianGrid, i1, i2, ...)\n\nReturn the coordinates of the node at multi-index I as an SVector. I must be a valid node index (i.e. I ∈ nodeindices(g)).\n\n\n\n\n\n","title":"LevelSetMethods.getnode"},{"category":"method","location":"reference/#LevelSetMethods.gradient-Tuple{InterpolatedField, Any}","page":"Reference","text":"gradient(cf::InterpolatedField, x)\n\nEvaluate the spatial gradient vector of the interpolated field at the point x.\n\n\n\n\n\n","title":"LevelSetMethods.gradient"},{"category":"method","location":"reference/#LevelSetMethods.gradient-Tuple{LevelSetMethods.BernsteinPolynomial, Any}","page":"Reference","text":"gradient(p::BernsteinPolynomial, x)\n\nCompute the gradient of p at point x using ForwardDiff.\n\n\n\n\n\n","title":"LevelSetMethods.gradient"},{"category":"method","location":"reference/#LevelSetMethods.gradient-Union{Tuple{N}, Tuple{LevelSetMethods.AbstractMeshField{N}, CartesianIndex}} where N","page":"Reference","text":"gradient(ϕ::AbstractMeshField, I::CartesianIndex)\n\nGradient ∇ϕ at grid index I from centered finite differences, returned as an SVector.\n\n\n\n\n\n","title":"LevelSetMethods.gradient"},{"category":"method","location":"reference/#LevelSetMethods.grid1d-Union{Tuple{CartesianGrid{N}}, Tuple{N}} where N","page":"Reference","text":"grid1d(g::CartesianGrid[, dim::Integer])\n\nReturn a LinRange of the coordinates along the given dimension dim. If dim is not provided, return a tuple of LinRanges for all dimensions.\n\n\n\n\n\n","title":"LevelSetMethods.grid1d"},{"category":"method","location":"reference/#LevelSetMethods.hausdorff_distance-Tuple{LevelSetMethods.NewtonSDF, LevelSetMethods.NewtonSDF}","page":"Reference","text":"hausdorff_distance(sdf₁::NewtonSDF, sdf₂::NewtonSDF) -> Float64\n\nApproximate the Hausdorff distance between the zero level sets Γ₁ and Γ₂ of sdf₁ and sdf₂. The symmetric Hausdorff distance is\n\nd_H(Γ₁, Γ₂) = max(max_{p ∈ Γ₁} d(p, Γ₂),  max_{p ∈ Γ₂} d(p, Γ₁))\n\nEach one-sided maximum is approximated by iterating over the interface sample points of one NewtonSDF and finding the closest point on the other interface via the Newton-Lagrange solver.\n\n\n\n\n\n","title":"LevelSetMethods.hausdorff_distance"},{"category":"method","location":"reference/#LevelSetMethods.hessian-Tuple{InterpolatedField, Any}","page":"Reference","text":"hessian(cf::InterpolatedField, x)\n\nEvaluate the spatial Hessian matrix of the interpolated field at the point x.\n\n\n\n\n\n","title":"LevelSetMethods.hessian"},{"category":"method","location":"reference/#LevelSetMethods.hessian-Tuple{LevelSetMethods.BernsteinPolynomial, Any}","page":"Reference","text":"hessian(p::BernsteinPolynomial, x)\n\nCompute the hessian of p at point x using ForwardDiff.\n\n\n\n\n\n","title":"LevelSetMethods.hessian"},{"category":"method","location":"reference/#LevelSetMethods.hessian-Union{Tuple{N}, Tuple{LevelSetMethods.AbstractMeshField{N}, CartesianIndex}} where N","page":"Reference","text":"hessian(ϕ::AbstractMeshField, I::CartesianIndex)\n\nHessian Hϕ = ∇∇ϕ at grid index I from second-order centered finite differences, returned as a Symmetric SMatrix.\n\n\n\n\n\n","title":"LevelSetMethods.hessian"},{"category":"function","location":"reference/#LevelSetMethods.integrate!","page":"Reference","text":"integrate!(ls::LevelSetEquation, tf, Δt = Inf; prehook = identity, posthook = identity)\n\nIntegrate the LevelSetEquation ls up to time tf, mutating the levelset and current_time of the object ls in the process.\n\nAn optional parameter Δt can be passed to specify a maximum time-step allowed for the integration. Note that the internal time-steps taken to evolve the level-set up to tf may be smaller than Δt due to stability reasons related to the terms and integrator employed.\n\nprehook and posthook are functions called once per accepted time step (as opposed to once per Runge-Kutta stage): prehook(ls) runs at the start of the step, before the state is advanced, and posthook(ls) runs after the step has been committed. Both receive ls with current_state(ls) and current_time(ls) synced to that point, and may mutate current_state(ls) (mutations are carried into the step). Their return values are ignored; the default identity is a no-op.\n\nReinitialization of the level-set to a signed distance function is not built in by default, but can be easily added by creating a prehook: pass prehook = eq -> reinitialize!(current_state(eq)) (see reinitialize!) to reinitialize before every step, or gate the call on any criterion (elapsed time, |∇ϕ| drift, a step counter closed over by the hook). The integrator never reinitializes on its own — keeping a signed distance function is entirely the caller's choice.\n\n\n\n\n\n","title":"LevelSetMethods.integrate!"},{"category":"method","location":"reference/#LevelSetMethods.isrealvalued-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"isrealvalued(ϕ::AbstractMeshField)\n\nReturn true if the values of ϕ are real numbers. Real-valued fields are the ones that may be interpreted as a level set (with their zero contour representing an interface).\n\n\n\n\n\n","title":"LevelSetMethods.isrealvalued"},{"category":"method","location":"reference/#LevelSetMethods.local_interpolant-Tuple{InterpolatedField, Any}","page":"Reference","text":"local_interpolant(cf::InterpolatedField, x)\n\nReturn the local BernsteinPolynomial representing the field in the cell containing the point x.\n\nwarning: Aliased coefficients\nThe returned polynomial aliases the calling task's scratch buffer; see make_interpolant.\n\n\n\n\n\n","title":"LevelSetMethods.local_interpolant"},{"category":"method","location":"reference/#LevelSetMethods.make_interpolant-Tuple{InterpolatedField, CartesianIndex}","page":"Reference","text":"make_interpolant(cf::InterpolatedField, I::CartesianIndex)\n\nReturn a BernsteinPolynomial for the cell at multi-index I, lazily computing (and caching) its Bernstein coefficients from the surrounding stencil of grid values.\n\nwarning: Aliased coefficients\nThe returned polynomial's coeffs array aliases the calling task's scratch buffer. It remains valid until the same task calls make_interpolant on cf with a different cell index (or cf is mutated). Copy coefficients(p) if the polynomial must outlive the current cell iteration.\n\n\n\n\n\n","title":"LevelSetMethods.make_interpolant"},{"category":"method","location":"reference/#LevelSetMethods.makie_theme-Tuple","page":"Reference","text":"makie_theme()\n\nReturn a Makie theme for plots of level-set functions.\n\nnote: Note\nRequires loading Makie.jl to activate the extension.\n\n\n\n\n\n","title":"LevelSetMethods.makie_theme"},{"category":"method","location":"reference/#LevelSetMethods.mesh-Tuple{LevelSetEquation}","page":"Reference","text":"mesh(eq::LevelSetEquation)\n\nReturn the underlying CartesianGrid of the equation.\n\n\n\n\n\n","title":"LevelSetMethods.mesh"},{"category":"method","location":"reference/#LevelSetMethods.meshsize-Tuple{CartesianGrid}","page":"Reference","text":"meshsize(g::CartesianGrid[, dim::Integer])\n\nReturn the spacing between grid nodes along the given dimension dim. If dim is not provided, return a SVector of spacings for all dimensions.\n\n\n\n\n\n","title":"LevelSetMethods.meshsize"},{"category":"method","location":"reference/#LevelSetMethods.nlayers-Tuple{NarrowBandMeshField}","page":"Reference","text":"nlayers(nb::NarrowBandMeshField)\n\nReturn the halo depth of the band (see NarrowBandMeshField).\n\n\n\n\n\n","title":"LevelSetMethods.nlayers"},{"category":"method","location":"reference/#LevelSetMethods.nodeindices-Tuple{CartesianGrid}","page":"Reference","text":"nodeindices(g::CartesianGrid)\n\nReturn a CartesianIndices ranging over all node indices of g. Nodes are indexed 1:n[d] in each dimension d.\n\n\n\n\n\n","title":"LevelSetMethods.nodeindices"},{"category":"method","location":"reference/#LevelSetMethods.nodeindices-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"nodeindices(ϕ::AbstractMeshField)\n\nReturn all node indices of the underlying mesh. See active_nodeindices for the subset a field is actually defined on.\n\n\n\n\n\n","title":"LevelSetMethods.nodeindices"},{"category":"method","location":"reference/#LevelSetMethods.normal-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex}","page":"Reference","text":"normal(ϕ::AbstractMeshField, I::CartesianIndex)\n\nUnit exterior normal n = ∇ϕ / ‖∇ϕ‖ of the level set of ϕ at grid index I.\n\n\n\n\n\n","title":"LevelSetMethods.normal"},{"category":"method","location":"reference/#LevelSetMethods.perimeter-Tuple{MeshField}","page":"Reference","text":"perimeter(ϕ::MeshField)\n\nMeasure of the interface {x : ϕ(x) = 0} described by the zero level set of ϕ. In N dimensions this is the (N-1)-dimensional measure — a perimeter (arc length) in 2D, a surface area in 3D — approximated as ∫ δ(ϕ) ‖∇ϕ‖ dx with a smoothed Dirac delta δ. Contributions from the domain border are neglected.\n\nusing LevelSetMethods\nR = 0.5\nS₀ = 2π * R\ngrid = CartesianGrid((-1, -1), (1, 1), (200, 200))\nϕ = MeshField(x -> sqrt(sum(abs2, x)) - R, grid)\nLevelSetMethods.perimeter(ϕ), S₀\n\n# output\n\n(3.1426415491430384, 3.141592653589793)\n\n\n\n\n\n","title":"LevelSetMethods.perimeter"},{"category":"method","location":"reference/#LevelSetMethods.perimeter-Tuple{NarrowBandMeshField}","page":"Reference","text":"perimeter(nb::NarrowBandMeshField)\n\nMeasure of the interface {x : ϕ(x) = 0} described by the zero level set, matching perimeter(ϕ::MeshField). The smoothed Dirac delta is supported only within δmin of the interface — entirely inside the band — so the sum runs over the active nodes alone.\n\n\n\n\n\n","title":"LevelSetMethods.perimeter"},{"category":"method","location":"reference/#LevelSetMethods.proven_empty-Tuple{InterpolatedField, CartesianIndex}","page":"Reference","text":"proven_empty(cf::InterpolatedField, I::CartesianIndex; surface=false)\n\nReturn true if cell I is guaranteed to contain no interface (when surface=true) or no interior (when surface=false), based on the convex-hull property of the Bernstein basis.\n\n\n\n\n\n","title":"LevelSetMethods.proven_empty"},{"category":"method","location":"reference/#LevelSetMethods.quadrature-Tuple{Any}","page":"Reference","text":"quadrature(mf::AbstractMeshField; interpolation_order, quadrature_order, surface=false)\nquadrature(ϕ::InterpolatedField; quadrature_order, surface=false)\n\nGenerate a quadrature for the implicit domain defined by the level set. If surface=true, generate a quadrature for the interface ϕ=0; otherwise for the interior ϕ < 0.\n\nThe first form wraps mf in an InterpolatedField of degree interpolation_order and quadratures over it; pass an InterpolatedField directly (second form) to reuse an existing interpolant and skip interpolation_order. For a high-order rate, keep quadrature_order ≥ interpolation_order so the rule does not cap the interpolant's accuracy.\n\nReturns a Dict mapping each cut cell's CartesianIndex to its ImplicitIntegration.Quadrature (which exposes coords and weights); provably empty cells are omitted.\n\nnote: Note\nRequires loading ImplicitIntegration.jl to activate the extension.\n\n\n\n\n\n","title":"LevelSetMethods.quadrature"},{"category":"method","location":"reference/#LevelSetMethods.reinitialize!-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"reinitialize!(ϕ::AbstractMeshField; order=3, upsample=2, maxiters=20, xtol=nothing, ftol=nothing)\n\nReinitialize the level set ϕ to a signed distance function in place using the Newton closest-point method: a NewtonSDF is built from ϕ and each active grid node is overwritten with its signed distance to the interface (a Newton-Lagrange solve seeded from the nearest interface sample). The keyword arguments are forwarded to NewtonSDF.\n\nReinitialization is not built into the time integrator; drive it from a prehook, e.g. prehook = eq -> reinitialize!(current_state(eq); order = 5). See integrate!.\n\n\n\n\n\n","title":"LevelSetMethods.reinitialize!"},{"category":"method","location":"reference/#LevelSetMethods.set_makie_theme!-Tuple","page":"Reference","text":"set_makie_theme!()\n\nSet the Makie theme to LevelSetMethods.makie_theme().\n\nnote: Note\nRequires loading Makie.jl to activate the extension.\n\n\n\n\n\n","title":"LevelSetMethods.set_makie_theme!"},{"category":"method","location":"reference/#LevelSetMethods.terms-Tuple{Any}","page":"Reference","text":"terms(eq::LevelSetEquation)\n\nReturn the tuple of LevelSetTerms of the equation.\n\n\n\n\n\n","title":"LevelSetMethods.terms"},{"category":"method","location":"reference/#LevelSetMethods.time_integrator-Tuple{Any}","page":"Reference","text":"time_integrator(eq::LevelSetEquation)\n\nReturn the TimeIntegrator of the equation.\n\n\n\n\n\n","title":"LevelSetMethods.time_integrator"},{"category":"method","location":"reference/#LevelSetMethods.update_band!-Tuple{LevelSetMethods.AbstractMeshField}","page":"Reference","text":"update_band!(ϕ::AbstractMeshField; nlayers)\n\nRebuild the active node set topologically: every node within nlayers axis-aligned steps of a cut cell (one whose corner values straddle zero). ϕ must be real-valued (a level set). Values are filled via ϕ's own indexing (stored value or affine extrapolation).\n\nnlayers defaults to the band's stored halo depth (see NarrowBandMeshField).\n\nA full-grid MeshField already holds every node, so there is no band to rebuild and this is a no-op — making it safe to call regardless of the state type. integrate! calls it automatically after each step.\n\n\n\n\n\n","title":"LevelSetMethods.update_band!"},{"category":"method","location":"reference/#LevelSetMethods.update_term!-Tuple{LevelSetMethods.LevelSetTerm, Any, Any}","page":"Reference","text":"update_term!(term::LevelSetTerm, ϕ, t)\n\nUpdate the internal state of a LevelSetTerm before computing its contribution. Called before the CFL estimate and at each stage of the time integration.\n\n\n\n\n\n","title":"LevelSetMethods.update_term!"},{"category":"method","location":"reference/#LevelSetMethods.value_and_gradient-Tuple{InterpolatedField, Any}","page":"Reference","text":"value_and_gradient(cf::InterpolatedField, x)\n\nEvaluate the value and spatial gradient vector of the interpolated field at the point x.\n\n\n\n\n\n","title":"LevelSetMethods.value_and_gradient"},{"category":"method","location":"reference/#LevelSetMethods.value_and_gradient-Tuple{LevelSetMethods.BernsteinPolynomial, Any}","page":"Reference","text":"value_and_gradient(p::BernsteinPolynomial, x)\n\nFused computation of the value and gradient of p at point x using ForwardDiff.\n\n\n\n\n\n","title":"LevelSetMethods.value_and_gradient"},{"category":"method","location":"reference/#LevelSetMethods.value_gradient_hessian-Tuple{InterpolatedField, Any}","page":"Reference","text":"value_gradient_hessian(cf::InterpolatedField, x)\n\nEvaluate the value, spatial gradient vector, and Hessian matrix of the interpolated field at the point x.\n\n\n\n\n\n","title":"LevelSetMethods.value_gradient_hessian"},{"category":"method","location":"reference/#LevelSetMethods.value_gradient_hessian-Tuple{LevelSetMethods.BernsteinPolynomial, Any}","page":"Reference","text":"value_gradient_hessian(p::BernsteinPolynomial, x)\n\nFused computation of the value, gradient, and hessian of p at point x using ForwardDiff.\n\n\n\n\n\n","title":"LevelSetMethods.value_gradient_hessian"},{"category":"method","location":"reference/#LevelSetMethods.volume-Tuple{MeshField}","page":"Reference","text":"volume(ϕ::MeshField)\n\nMeasure of the region {x : ϕ(x) ≤ 0} enclosed by the zero level set of ϕ. In N dimensions this is the N-dimensional measure — a length in 1D, an area in 2D, a volume in 3D — approximated as ∫ H(-ϕ) dx with a smoothed Heaviside H.\n\nusing LevelSetMethods\nR = 0.5\nV₀ = π * R^2\ngrid = CartesianGrid((-1, -1), (1, 1), (200, 200))\nϕ = MeshField(x -> sqrt(sum(abs2, x)) - R, grid)\nLevelSetMethods.volume(ϕ), V₀\n\n# output\n\n(0.7854362890190668, 0.7853981633974483)\n\n\n\n\n\n","title":"LevelSetMethods.volume"},{"category":"method","location":"reference/#LevelSetMethods.volume-Union{Tuple{NarrowBandMeshField{N}}, Tuple{N}} where N","page":"Reference","text":"volume(nb::NarrowBandMeshField)\n\nMeasure of the region {x : ϕ(x) ≤ 0} enclosed by the zero level set, matching volume(ϕ::MeshField) but computed from the band alone, without materialising values on the full grid.\n\nThe smoothed Heaviside transition is supported within δmin of the interface — entirely inside the band — so every off-band node contributes exactly 0 (outside) or 1 (inside), and only its sign is needed. Those signs are recovered by a scanline sweep: the interface never crosses outside the band, so along each grid line the off-band interior shows up as same-sign gaps between consecutive band nodes (and the tails beyond the outermost ones), counted by arithmetic without ever visiting an interior node. A line with no band node carries no crossing and is classified by the sign of its nearest band node.\n\n\n\n\n\n","title":"LevelSetMethods.volume"},{"category":"method","location":"reference/#LevelSetMethods.weno5⁺-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Int64}","page":"Reference","text":"weno5⁺(ϕ, I, dim)\n\nFifth-order WENO (Weighted Essentially Non-Oscillatory) reconstruction of the derivative at grid point I along dimension dim, using a right-biased stencil.\n\n\n\n\n\n","title":"LevelSetMethods.weno5⁺"},{"category":"method","location":"reference/#LevelSetMethods.weno5⁻-Tuple{LevelSetMethods.AbstractMeshField, CartesianIndex, Int64}","page":"Reference","text":"weno5⁻(ϕ, I, dim)\n\nFifth-order WENO (Weighted Essentially Non-Oscillatory) reconstruction of the derivative at grid point I along dimension dim, using a left-biased stencil.\n\n\n\n\n\n","title":"LevelSetMethods.weno5⁻"},{"category":"method","location":"reference/#LevelSetMethods.export_surface_mesh-Tuple{LevelSetEquation, Vararg{Any}}","page":"Reference","text":"export_surface_mesh(eq::LevelSetEquation, args...; kwargs...)\n\nCall export_surface_mesh on current_state(eq).\n\n\n\n\n\n","title":"LevelSetMethods.export_surface_mesh"},{"category":"method","location":"reference/#LevelSetMethods.export_surface_mesh-Tuple{MeshField, String}","page":"Reference","text":"export_surface_mesh(ϕ::MeshField, output::String;\n    hgrad = nothing, hmin = nothing, hmax = nothing, hausd = nothing)\n\nCompute a mesh of the MeshField ϕ zero contour using MMGs_O3.\n\nhgrad control the growth ratio between two adjacent edges\n\nhmin and hmax control the edge sizes to be (respectively) greater than the hmin parameter and lower than the hmax one\n\nhausd control the maximal distance between the piecewise linear representation of the boundary and the reconstructed ideal boundary\n\nnote: Note\nOnly works for 3 dimensional level-set.\n\n\n\n\n\n","title":"LevelSetMethods.export_surface_mesh"},{"category":"method","location":"reference/#LevelSetMethods.export_volume_mesh-Tuple{LevelSetEquation, Vararg{Any}}","page":"Reference","text":"export_volume_mesh(eq::LevelSetEquation, output; kwargs...)\n\nCall export_volume_mesh on current_state(eq).\n\n\n\n\n\n","title":"LevelSetMethods.export_volume_mesh"},{"category":"method","location":"reference/#LevelSetMethods.export_volume_mesh-Tuple{MeshField, String}","page":"Reference","text":"export_volume_mesh(ϕ::MeshField, output::String;\n    hgrad = nothing, hmin = nothing, hmax = nothing, hausd = nothing)\n\nCompute a mesh of the domains associated with MeshField eq using either MMG2dO3 or MMG3dO3.\n\nhgrad control the growth ratio between two adjacent edges.\n\nhmin and hmax control the edge sizes to be (respectively) greater than the hmin parameter and lower than the hmax one.\n\nhausd control the maximal distance between the piecewise linear representation of the boundary and the reconstructed ideal boundary.\n\nFor more information, see the official MMG documentation.\n\nnote: Note\nOnly works for 2 and 3 dimensional level-set.\n\n\n\n\n\n","title":"LevelSetMethods.export_volume_mesh"},{"category":"section","location":"example-zalesak/#zalesak","page":"Zalesak disk","text":"The Zalesak disk — a slotted disk in solid-body rotation — is a standard benchmark for advection schemes. We set it up and evolve it below.","title":"Zalesak disk"},{"category":"section","location":"example-zalesak/#Setting-up-the-grid-and-disk","page":"Zalesak disk","text":"On a Cartesian grid, we carve a rectangular notch out of a disk to form the Zalesak disk.\n\ngrid = CartesianGrid((-1.5, -1.5), (1.5, 1.5), (100, 100))\ncenter, radius = (-0.75, 0), 0.5\nh, w = 1.0, 0.2                                   # notch height and width\ndisk = MeshField(x -> hypot((x .- center)...) - radius, grid)\nrec  = MeshField(x -> maximum(abs.(x .- (center .- (0, radius))) .- (w, h) ./ 2), grid)\nϕ = setdiff(disk, rec)                            # carve the notch out; see the geometry page\nplot(ϕ)\ncurrent_figure() # hide","title":"Setting up the grid and disk"},{"category":"section","location":"example-zalesak/#Setting-up-the-level-set-equation","page":"Zalesak disk","text":"We advect the disk with a rotational velocity field:\n\neq = LevelSetEquation(;\n    ic = ϕ,\n    terms = AdvectionTerm((x, t) -> (-x[2], x[1])),\n    bc = NeumannBC(),\n)","title":"Setting up the level-set equation"},{"category":"section","location":"example-zalesak/#Evolving-the-Zalesak-disk","page":"Zalesak disk","text":"We integrate the equation over a full revolution, recording the evolution with GLMakie:\n\nobs = Observable(eq)\nfig = Figure()\nax = Axis(fig[1, 1])\nplot!(ax, obs)\nframerate = 30\nt0 = current_time(eq)\ntf = 2*π\ntimestamps = range(t0, tf; step = 1 / framerate)\nrecord(fig, joinpath(@__DIR__, \"zalesak2d.gif\"), timestamps) do t_\n    integrate!(eq, t_)\n    return obs[] = eq\nend\n\n(Image: Zalesak 2D)\n\nWe see some small smearing of the disk due to numerical diffusion; this is a common issue, and the situation would be much worse with a low-order upwind scheme.","title":"Evolving the Zalesak disk"},{"category":"section","location":"example-zalesak/#A-three-dimensional-Zalesak-disk","page":"Zalesak disk","text":"The same example can be run in 3D, but the solution takes longer to compute and visualize.\n\nwarning: Performance Warning\nThe 3D example below may take a minute or two to run.\n\nusing LevelSetMethods, GLMakie, StaticArrays\nLevelSetMethods.set_makie_theme!()\n\ngrid = CartesianGrid((-1, -1, -1), (1, 1, 1), (50, 50, 50))\ncenter = (-1 / 3, 0, 0)\nradius = 0.5\ndisk = MeshField(x -> hypot((x .- center)...) - radius, grid)\nrec  = MeshField(x -> maximum(abs.(x .- (center .+ (0, radius, 0))) .- (1 / 3, 1.0, 2) ./ 2), grid)\nϕ = setdiff(disk, rec)\neq = LevelSetEquation(;\n    ic = ϕ,\n    terms = AdvectionTerm((x, t) -> π * SVector(x[2], -x[1], 0)),\n    bc = NeumannBC(),\n)\n\nobs = Observable(eq)\nfig = Figure()\nax = Axis3(fig[1, 1])\nplot!(ax, obs)\nframerate = 30\ntf = 2\ntimestamps = range(0, tf, tf * framerate)\nrecord(fig, joinpath(@__DIR__, \"zalesak3d.gif\"), timestamps) do t_\n    integrate!(eq, t_)\n    return obs[] = eq\nend\n\n(Image: Zalesak 3D)","title":"A three-dimensional Zalesak disk"},{"category":"section","location":"velocity-extension/#velocity-extension","page":"Velocity extension","text":"Sometimes the speed that moves the interface is known only on the interface: it comes from a model evaluated at the front — a physical flux, a curvature law, an optimisation sensitivity. To advance the level set you need that speed at every node the update touches, not just at the zero set. Velocity extension fills in the surrounding nodes by carrying the interface values outward along the normal direction.\n\nextend_along_normals! does this by solving, in pseudo-time, the transport equation\n\npartial_tau F + operatornamesign(phi) boldsymboln cdot nabla F = 0\nqquad boldsymboln = nablaphi  nablaphi\n\nwhich pushes F away from the interface along the normals until it is constant along each one — the construction of Peng et al. [5]. Because information flows outward from the front (the operatornamesign(phi) factor flips the upwind direction on each side), the interface values act as the boundary data and the rest of the field is overwritten.","title":"Velocity extension"},{"category":"section","location":"velocity-extension/#Extending-a-speed-off-the-interface","page":"Velocity extension","text":"The example below starts from a speed defined only in a thin band around a circle (here the polar angle theta) and extends it across the whole grid. Nodes inside the interface band are frozen by default — their values are the data being propagated — while everything else is filled in; pass frozen to choose the held nodes explicitly.\n\nusing LevelSetMethods\nusing CairoMakie\nusing StaticArrays\n\ngrid = CartesianGrid((-1, -1), (1, 1), (64, 64))\nϕ = MeshField(x -> hypot(x...) - 0.5, grid)          # a circle, as a signed distance function\nΔ = minimum(LevelSetMethods.meshsize(grid))\n\n# a speed known only near the interface (the polar angle); arbitrary elsewhere\nF = MeshField(grid; bc = LinearExtrapolationBC()) do x\n    abs(hypot(x...) - 0.5) <= 1.5Δ ? atan(x[2], x[1]) : 0.0\nend\nbefore = copy(values(F))\n\nextend_along_normals!(F, ϕ; nb_iters = 100)\n\nxs = ys = range(-1, 1; length = 64)\nLevelSetMethods.set_makie_theme!()\nfig = Figure(; size = (760, 330))\nax1 = Axis(fig[1, 1]; title = \"Known near interface\", aspect = 1)\nax2 = Axis(fig[1, 2]; title = \"Extended along normals\", aspect = 1, yticklabelsvisible = false)\nheatmap!(ax1, xs, ys, before; colormap = :twilight)\nhm = heatmap!(ax2, xs, ys, values(F); colormap = :twilight)\nfor ax in (ax1, ax2)\n    contour!(ax, xs, ys, values(ϕ); levels = [0.0], color = :white, linewidth = 2)\nend\nColorbar(fig[1, 3], hm)\nfig\n\nThe extended field is constant along each normal. Probing one ray at theta = pi4 at three different radii returns the same value — the interface speed, carried unchanged:\n\nFi = InterpolatedField(F, 1)\nvals = [Fi(SVector(r * cos(π / 4), r * sin(π / 4))) for r in (0.3, 0.5, 0.8)]\n@assert maximum(vals) - minimum(vals) < 1e-3 \"extension should be constant along the normal\"  # hide\n@assert all(abs.(vals .- π / 4) .< 1e-2) \"extension should carry the interface value θ = π/4\"   # hide\nvals","title":"Extending a speed off the interface"},{"category":"section","location":"velocity-extension/#Driving-a-normal-speed-term","page":"Velocity extension","text":"The most common use is to supply the speed of a NormalMotionTerm: extend the known interface speed onto the band, then hand the extended field to the term so the front moves consistently everywhere. See the level-set terms page for a worked example.\n\ntip: Cost and frequency\nEach call runs nb_iters upwind sweeps, so the extension reaches nb_iters cells from the interface. Use just enough iterations to cover the band the scheme reads from (a few cells for a standard stencil), and re-extend whenever the interface — and hence its speed — has moved appreciably, typically from the same posthook used for reinitialization.","title":"Driving a normal-speed term"},{"category":"section","location":"extension-implicit-integration/#extension-implicit-integration","page":"ImplicitIntegration extension","text":"Loading ImplicitIntegration activates LevelSetMethods.quadrature, which builds a high-order quadrature — a set of nodes and weights — for integrating over the implicit domain x  phi(x)  0 or its interface x  phi(x) = 0. This is the accurate counterpart to the smoothed measures of the Geometric quantities page: where volume and perimeter smear an indicator over a cell and are only low order, the quadrature here resolves the curved domain to the order you ask for.\n\nusing LevelSetMethods, ImplicitIntegration","title":"ImplicitIntegration extension"},{"category":"section","location":"extension-implicit-integration/#Building-a-quadrature","page":"ImplicitIntegration extension","text":"Call LevelSetMethods.quadrature directly on a MeshField, giving two orders: an interpolation_order for the geometry and a quadrature_order for the integration rule:\n\ngrid = CartesianGrid((-1.0, -1.0), (1.0, 1.0), (32, 32))\nmf = MeshField(x -> hypot(x...) - 0.5, grid)   # a disk of radius 0.5\nquad = LevelSetMethods.quadrature(mf; interpolation_order = 3, quadrature_order = 4)\nnothing # hide\n\nUnder the hood this wraps mf in an InterpolatedField of degree interpolation_order — the polynomial interpolant is what lets the algorithm locate and integrate over the zero contour to high order — and then builds a quadrature of order quadrature_order on each non-empty cell. The two are independent knobs: the interpolant fixes how faithfully the geometry is represented, the rule fixes how exactly polynomials are integrated on it. While quadrature_order can be taken (essentially) as large as you like, the interpolation order is limited by the grid resolution; on a uniform Cartesian grid it is advisable to keep it modest to avoid Runge-like phenomena. If you already hold an InterpolatedField (see Interpolation), pass it directly and drop interpolation_order.\n\nThe result is a Dict mapping each cut cell's CartesianIndex to its quadrature; provably empty cells (by the Bernstein convex-hull test, see Interpolation) are omitted. Each quadrature exposes its nodes as coords and the matching weights, so integrating a function f is the weighted sum int f approx sum_i f(x_i) w_i over every node of every cell:\n\nintegrate(f, quad) = sum(sum(f(x) * w for (x, w) in zip(Q.coords, Q.weights)) for Q in values(quad))\n\narea = integrate(_ -> 1.0, quad)                                    # ∫ 1 over the disk → πr²\n@assert abs(area - π * 0.5^2) / (π * 0.5^2) < 1e-5   # hide\nround(area; sigdigits = 6), round(π * 0.5^2; sigdigits = 6)\n\nEven on this coarse 32 times 32 grid the area matches pi r^2 to a part in a million. Passing surface = true integrates over the interface instead — here f equiv 1 gives the perimeter 2pi r:\n\nqsurf = LevelSetMethods.quadrature(mf; interpolation_order = 3, quadrature_order = 4, surface = true)\nperim = integrate(_ -> 1.0, qsurf)\n@assert abs(perim - 2π * 0.5) / (2π * 0.5) < 1e-5   # hide\nround(perim; sigdigits = 6), round(2π * 0.5; sigdigits = 6)","title":"Building a quadrature"},{"category":"section","location":"extension-implicit-integration/#Visualizing-the-nodes","page":"ImplicitIntegration extension","text":"It is often clearest to see where the nodes land. Here we build the quadrature on a Cassini oval — x - P_1x - P_2 = b^2 — plot the level set with the package's own plot! recipe, and scatter the interior nodes against the surface nodes on top:\n\nusing CairoMakie, LinearAlgebra, StaticArrays\nLevelSetMethods.set_makie_theme!()\n\nP1, P2 = SVector(-0.6, 0.0), SVector(0.6, 0.0)\ngridc = CartesianGrid((-1.0, -1.0), (1.0, 1.0), (50, 50))\nmfc = MeshField(x -> norm(x .- P1) * norm(x .- P2) - 0.62^2, gridc)\n\nvolpts = reduce(vcat, [Q.coords for Q in values(LevelSetMethods.quadrature(mfc; interpolation_order = 3, quadrature_order = 1))])\n\nfig = Figure()\nax = Axis(fig[1, 1]; title = \"Quadrature nodes on a Cassini oval\")\nplot!(ax, mfc)\nscatter!(ax, [Point2f(p) for p in volpts]; markersize = 3)\nfig\n\nThe interior nodes tile the domain enclosed by the oval, while the surface nodes trace the curve itself — each cluster is the quadrature for one cut cell.","title":"Visualizing the nodes"},{"category":"section","location":"extension-implicit-integration/#Limitations","page":"ImplicitIntegration extension","text":"Volume integrals (surface = false) are not supported on a NarrowBandMeshField: interior cells deep inside the zero level set lie outside the band and would be missed. Use a full MeshField for volume integrals; surface integrals work on a band, since the interface is always in the band.","title":"Limitations"},{"category":"section","location":"geometry-queries/#geometry-queries","page":"Geometric quantities","text":"Once you have a level set — whether freshly built (see Level sets) or evolved through a LevelSetEquation — you often want to read geometry off it. There are two flavours of query: global measures of the shape it encodes (the size of the enclosed region, the length or area of the interface) and local differential geometry at a point (the outward normal, the curvature). Both are read-only — they compute from phi without modifying it — and both work on a dense MeshField and a NarrowBandMeshField alike.","title":"Geometric quantities"},{"category":"section","location":"geometry-queries/#Global-measures:-volume-and-perimeter","page":"Geometric quantities","text":"For a level set in N dimensions, LevelSetMethods.volume returns the N-dimensional measure of the enclosed region x  phi(x) le 0 (a length in 1D, an area in 2D, a volume in 3D), and LevelSetMethods.perimeter the (N-1)-dimensional measure of the interface x  phi(x) = 0. They are computed by integrating a smoothed indicator over the grid, int H(-phi)mathrmdx and int delta(phi)lvertnablaphirvertmathrmdx respectively. Neither is exported. As a check, the area and circumference of a disk of radius 05 come out close to the exact pi r^2 and 2pi r:\n\nusing LevelSetMethods\ngrid = CartesianGrid((-1, -1), (1, 1), (64, 64))\nϕ = MeshField(x -> hypot(x...) - 0.5, grid)\n(LevelSetMethods.volume(ϕ), π * 0.5^2), (LevelSetMethods.perimeter(ϕ), 2π * 0.5)\n\n(perimeter reads nablaphi with a centered stencil that reaches off-grid at the border; if the field carries no boundary conditions a default linear extrapolation is supplied, so the call works without a bc.)\n\nBecause the smoothed Heaviside and Dirac are spread over roughly one cell width, both measures are low-order approximations: convenient monitoring quantities that converge under refinement, rather than high-accuracy integrals.\n\nerr(n) = abs(LevelSetMethods.volume(MeshField(x -> hypot(x...) - 0.5,\n            CartesianGrid((-1, -1), (1, 1), (n, n)))) - π * 0.5^2)\n@assert err(128) < err(32)   # hide  (error shrinks under refinement)\nerr(32), err(128)\n\nWhen you need an accurate integral over the implicit domain or its interface — rather than an eyeball-grade measure — use the high-order quadrature from the ImplicitIntegration extension instead. Tracking volume (or perimeter) across a simulation is the usual way to monitor mass conservation; drive it from a posthook on integrate!, as shown on the Level-set equation page.","title":"Global measures: volume and perimeter"},{"category":"section","location":"geometry-queries/#Local-differential-geometry","page":"Geometric quantities","text":"At a grid node you can query the differential geometry of the level set directly. The gradient nablaphi gives the (unnormalised) direction of steepest ascent; LevelSetMethods.normal normalises it to the outward unit normal boldsymboln = nablaphi  lvertnablaphirvert; and LevelSetMethods.curvature returns the mean curvature kappa = nabla cdot (nablaphi  lvertnablaphirvert). All three are built from centered finite differences (via LevelSetMethods.hessian for the second-order part) and take a CartesianIndex. For the disk above, the node at (05 0) has an outward normal along +x and a curvature close to 1r = 2:\n\ngc = CartesianGrid((-1, -1), (1, 1), (65, 65))\nϕc = MeshField(x -> hypot(x...) - 0.5, gc)\nI = CartesianIndex(49, 33)                # the node at (0.5, 0.0)\nLevelSetMethods.normal(ϕc, I), LevelSetMethods.curvature(ϕc, I)\n\nBecause these are finite differences, they are most reliable where phi is well resolved and close to a signed distance function. Curvature in particular is a second-derivative quantity, sensitive to noise in phi; keeping the level set reinitialized (see Reinitialization) keeps it well behaved, and it is defined to be zero wherever nablaphi vanishes. To evaluate the field or its derivatives at arbitrary points rather than at nodes, wrap it in an InterpolatedField, which differentiates the local polynomial patch exactly; see Interpolation.","title":"Local differential geometry"},{"category":"section","location":"#LevelSetMethods","page":"Home","text":"LevelSetMethods.jl is a Julia package for representing and evolving implicitly defined domains in mathbbR^d. Rather than tracking the boundary of a domain Omega directly, it represents Omega as the sub-zero region of a scalar level-set function phi,\n\nOmega = leftboldsymbolx in mathbbR^d  phi(boldsymbolx)  0 right\n\nwith the interface partialOmega recovered as the zero contour phi = 0. Moving the interface then amounts to evolving phi under a partial differential equation. Because the interface is never meshed or tracked explicitly, topological changes — merging, splitting, pinching off — happen automatically, which is the central appeal of the level-set method.","title":"LevelSetMethods"},{"category":"section","location":"#Installation","page":"Home","text":"To install the library, run the following command on a Julia REPL:\n\nusing Pkg; Pkg.add(\"LevelSetMethods\")\n\nThis will install the latest tagged version of the package and its dependencies.\n\nFor visualization, you may also want to install a Makie backend: CairoMakie is a good default for 2D figures and animations, while GLMakie is needed for the 3D isosurface plots.","title":"Installation"},{"category":"section","location":"#Overview","page":"Home","text":"This package defines a LevelSetEquation type that can be used to solve partial differential equations of the form\n\nphi_t + underbraceboldsymbolu cdot nabla phi_substacktextadvection  textterm + underbracev nabla phi_substacktextnormal  textterm + underbraceb kappa nabla phi_substacktextcurvature  textterm + underbracetextsign(phi)(nabla phi - 1)_substacktextreinitialization textterm = 0\n\nwhere\n\nphi  mathbbR^d times mathbbR^+ to mathbbR is the level set function\nboldsymbolu mathbbR^d times mathbbR^+  to mathbbR^d is a given (external) velocity field\nv  mathbbR^d times mathbbR^+ to mathbbR is a normal speed\nb  mathbbR^d times mathbbR^+ to mathbbR is a function that multiplies the curvature kappa = nabla cdot (nabla phi  nabla phi)\n\nHere is how it looks in practice. We rotate a dumbbell — assembled from two disks and a bar with the set operations of the geometry page — about the origin:\n\nusing LevelSetMethods\ngrid = CartesianGrid((-1, -1), (1, 1), (50, 50))\ndisk(c) = MeshField(x -> hypot((x .- c)...) - 0.25, grid)\nbar = MeshField(x -> maximum(abs.(x) .- (1.0, 0.2) ./ 2), grid)\nϕ = disk((-0.5, 0.0)) ∪ disk((0.5, 0.0)) ∪ bar\n𝐮 = (x, t) -> (-x[2], x[1])\neq = LevelSetEquation(; terms = (AdvectionTerm(𝐮),), ic = ϕ, bc = NeumannBC())\n\nLoading a Makie backend lets you plot the equation with plot:\n\nusing CairoMakie # loads the MakieExt from LevelSetMethods\nLevelSetMethods.set_makie_theme!() # optional theme customization\nplot(eq)\n\nintegrate! advances the equation in place. Calling it repeatedly at increasing times is the idiom behind animations; we wrap that loop in a small helper that writes the current current_time into the axis title. The recipe only draws the state — Makie recipes cannot set the axis title — so the title is the caller's job; we format it with a fixed two decimals (@sprintf) so its width stays constant and the label does not flicker:\n\nusing Printf\nfunction animate(eq, filename; tf = π)\n    obs = Observable(eq)\n    fig = Figure()\n    ax = Axis(fig[1, 1])\n    plot!(ax, obs)\n    on(obs) do e\n        ax.title = @sprintf(\"t = %.2f\", current_time(e))\n    end\n    record(fig, joinpath(@__DIR__, filename), range(0, tf; step = 1 / 30)) do t\n        integrate!(eq, t)\n        obs[] = eq\n    end\n    return nothing\nend\n\nanimate(eq, \"ls_intro.gif\")\nnothing # hide\n\n(Image: Dumbbell)\n\nNote that ic is copied into the equation, so the field ϕ you passed in is left untouched — the evolving state lives in current_state.\n\ntip: Keeping a signed distance function\nCurvature, normals, and narrow bands all behave best when phi is a signed distance function (nablaphi = 1), a property advection steadily distorts. The usual remedy is to reinitialize between steps by passing a posthook to integrate!:integrate!(eq, tf; posthook = eq -> reinitialize!(current_state(eq)))A PDE-based EikonalReinitializationTerm is also available. See Reinitialization for the trade-offs and details.\n\nThat same equation runs unchanged on a narrow band. Because the interface fills only a thin region of the domain, storing phi at every grid node is wasteful — especially in 3D. A NarrowBandMeshField keeps values only on a band of nodes around the interface, and is a drop-in replacement for a MeshField: swap it in as the initial condition and everything else — the terms, the boundary conditions, even the animate call above — stays exactly the same.\n\nnb = NarrowBandMeshField(disk((-0.5, 0.0)) ∪ disk((0.5, 0.0)) ∪ bar; nlayers = 3)\neq_band = LevelSetEquation(; terms = (AdvectionTerm(𝐮),), ic = nb, bc = NeumannBC())\nanimate(eq_band, \"ls_intro_band.gif\")\nnothing # hide\n\n(Image: Dumbbell on a narrow band)\n\nThe recipe shades the active band cells, which travel along with the interface; only those nodes are stored and advanced, so the cost scales with the size of the interface rather than the grid — a substantial saving in 3D. See Narrow-band fields for details.\n\nnote: Other resources\nThere is an almost one-to-one correspondence between each of the LevelSetTerms described above and individual chapters of the book by Osher and Fedkiw on level set methods [1], so users interested in digging deeper into the theory/algorithms are encouraged to consult that reference. We also drew some inspiration from the great Matlab library ToolboxLS by Ian Mitchell [2].","title":"Overview"},{"category":"section","location":"#extensions","page":"Home","text":"Some features of LevelSetMethods.jl are only available through extensions after loading certain optional dependencies:\n\nMakie: Loading a Makie backend (like GLMakie or CairoMakie) enables plotting recipes for level sets and equations. See Makie extension.\nMMG: Loading MMG_jll and MarchingCubes enables exporting level sets as volume or surface meshes. See MMG extension.\nImplicitIntegration: Loading ImplicitIntegration enables high-order quadratures over the implicit domain and its interface. See ImplicitIntegration extension.","title":"Extensions"},{"category":"section","location":"#Going-further","page":"Home","text":"The LevelSetEquation type seen above is the heart of the package, and the rest of the manual is organized around it; its docstrings are worth reading in detail.\n\nThe remaining documentation is grouped in the navigation sidebar:\n\nBuilding & solving covers each ingredient of an equation — grids and fields, level sets, terms, time integrators, boundary conditions — one page apiece.\nAdvanced topics goes beyond the basics.\nExtensions documents the optional features behind extra dependencies (see Extensions above).\nExamples works through complete applications end to end.\n\nEvery exported name is documented in the Reference.","title":"Going further"},{"category":"section","location":"example-shape-optim/#example-shape-optim","page":"Shape optimization: a primer","text":"","title":"Shape optimization: a primer"},{"category":"section","location":"example-shape-optim/#Isoperimetric-inequality-as-a-shape-optimization-problem","page":"Shape optimization: a primer","text":"We consider in this example the isoperimetric inequality which states that among all closed surfaces enclosing a fixed volume V_0  0, the sphere is the one with minimal perimeter. We show here how to demonstrate this result through numerical optimization.\n\nwarning: Warning\nThis example is purely illustrative. The optimization method used here has not been extensively tested. Coupling the LevelSetMethods toolbox to any simulation package makes it possible to solve PDE-constrained optimization problems (see for instance [6]).\n\nTo do this, we first define the problem mathematically:\n\n    beginarrayrl\n        displaystylemin_Omega subset mathbbR^d  P(Omega)\n        \n        textst  V(Omega) = V_0\n    endarrayqquadtext(1)\n\nwhere P(Omega) V(Omega) are the perimeter and volume of Omega defined by\n\n    V(Omega) = int_Omega textdmathbfx\n    quadtextandquad\n    P(Omega) = int_partial Omega textdmathbfs\n\nThe optimization problem text(1) can be solved using the augmented Lagrangian approach by minimizing iteratively the following functional:\n\n    f(Omega) = P(Omega) + lambda (V(Omega) - V_0) + fracmu2 (V(Omega) - V_0)^2\n    qquadtext(2)\n\nwhere mu is a parameter updated during the course of the optimization. To minimize text(2), we use a gradient-based algorithm. For this, we need to define what a small variation of Omega is. As such, for any shape Omega subset mathbbR^d we define (following the Hadamard method) its deformation Omega_boldsymboltheta by a small vector field boldsymboltheta in W^1infty(mathbbR^d mathbbR^d) as:\n\n    Omega_boldsymboltheta\n    = (textId + boldsymboltheta)(Omega)\n    = mathbfx + boldsymboltheta(mathbfx) mathbfx in Omega\n\nThe following first-order Taylor expansion can then be obtained:\n\n    f(Omega_boldsymboltheta)\n    =\n    f(Omega)\n    +\n    int_partial Omega\n    left(\n        kappa + (lambda + mu (V(Omega) - V_0))\n    right) boldsymboltheta cdot mathbfn\n    textdmathbfs\n    + o(boldsymboltheta)\n    \n\nIn other words, using boldsymboltheta = - (kappa + (lambda + mu (V(Omega) - V_0))) mathbfn and a small enough coefficient tau  0, f(Omega_tauboldsymboltheta) is necessarily smaller than f(Omega).","title":"Isoperimetric inequality as a shape optimization problem"},{"category":"section","location":"example-shape-optim/#Numerical-solution-using-the-level-set-method","page":"Shape optimization: a primer","text":"If Omega is given by the level-set function phi_0  mathbbR^d to mathbbR then one associated with Omega_tauboldsymboltheta is given by phi(cdot tau), solution of\n\n    partial_t phi - kappa nabla phi - (lambda + mu (V(Omega) - V_0)) nabla phi = 0\n\nwith phi(cdot t = 0) = phi_0. The descent speed -(lambda + mu(V - V_0)) depends on the current volume, so we let it track the shape as it moves and update the multipliers lambda mu once per outer iteration. Two features of the library make this clean: a NormalMotionTerm can refresh its own speed from the current level set through an update hook called before every stage, and integrate! chooses stable time steps automatically — so there is no manual time-stepping to manage.\n\nThis optimization method is implemented in the following Julia code:\n\nusing LevelSetMethods, LinearAlgebra, CairoMakie\nLevelSetMethods.set_makie_theme!()\n\ngrid = CartesianGrid((-1.0, -1.0), (1.0, 1.0), (50, 50))\n\nic = MeshField(grid) do x # a star; see the geometry page\n    r, θ = norm(x), atan(x[2], x[1])\n    return r - (1 + 0.25 * cos(5θ))\nend\n\nV0 = 0.5                            # target area\nλ, μ, c = Ref(0.0), Ref(0.1), 1.1  # multiplier, penalty weight, penalty growth\n\n# evolution ϕₜ - κ|∇ϕ| - (λ + μ(V - V₀))|∇ϕ| = 0: a fixed mean-curvature term plus a\n# normal-motion term whose speed tracks the current volume, refreshed before each stage by\n# the term's update hook\nspeed = MeshField(x -> 0.0, grid)\nupdate_speed!(v, ϕ, t) = fill!(values(v), -(λ[] + μ[] * (LevelSetMethods.volume(ϕ) - V0)))\neq = LevelSetEquation(;\n    terms = (NormalMotionTerm(speed, update_speed!), CurvatureTerm(MeshField(x -> -1.0, grid))),\n    ic,\n    bc = LinearExtrapolationBC(),\n)\n\nobs = Observable(eq)\nfig = Figure()\nax = Axis(fig[1, 1])\nplot!(ax, obs)\narc!(ax, Point2f(0, 0), sqrt(V0 / π), 0, 2π; linestyle = :dash, color = :red) # optimal disk\n\n# each frame advances the shape under the current multipliers, then takes one dual-ascent step\nrecord(fig, joinpath(@__DIR__, \"optimization.gif\"), range(0, 0.06; length = 100)) do t\n    integrate!(eq, t, 0.1)\n    V = LevelSetMethods.volume(eq)\n    λ[] += μ[] * (V - V0)\n    μ[] *= c\n    obs[] = eq\nend\n\n(Image: Optimization)\n\nThe multipliers lambda and mu (and the growth factor c) trade off how much the optimization focuses on minimizing the objective versus satisfying the constraint. Refining the grid sharpens the final shape further, since the curvature that drives the rounding is then better resolved.","title":"Numerical solution using the level-set method"}]
}
